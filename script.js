// Telegram WebApp API
const tg = window.Telegram.WebApp;

// Инициализация Telegram WebApp
tg.ready();
tg.expand();

// Глобальные переменные
let userInfo = {};
let currentTest = null;
let currentQuestion = 0;
let testAnswers = {};

// Переменные для теста выгорания
let burnoutCurrentQuestion = 0;
let burnoutAnswers = {};

// Переменные для теста тревоги
let anxietyCurrentQuestion = 0;
let anxietyAnswers = {};
let anxietyCurrentScale = 'state'; // 'state' или 'trait'

// Переменные для теста депрессии
let depressionCurrentQuestion = 0;
let depressionAnswers = {};

// Переменные для теста на буллинг
let bullyingCurrentSection = 0;
let bullyingCurrentQuestion = 0;
let bullyingAnswers = {};

// Переменные для теста Д. Олвеуса "Буллинг"
let olweusBullyingCurrentQuestion = 0;
let olweusBullyingAnswers = {};

// Переменные для теста агрессивного поведения
let aggressionCurrentQuestion = 0;
let aggressionAnswers = {};

// Функция для отправки результатов теста в Telegram (только админу)
async function sendTestResultsToTelegram(results, userInfo, testName) {
    try {
        // Получаем информацию о пользователе из Telegram
        const telegramUser = tg.initDataUnsafe?.user;
        const userName = telegramUser ? `${telegramUser.first_name} ${telegramUser.last_name || ''}`.trim() : 'Неизвестный пользователь';
        const userId = telegramUser?.id || 'unknown';
        const username = telegramUser?.username ? `@${telegramUser.username}` : 'Нет username';

        let message = `
📊 <b>Результаты теста ${testName}</b>

👤 <b>Данные пользователя:</b>
• Имя: ${userName}
• Username: ${username}
• Telegram ID: ${userId}
• Пол: ${userInfo.gender === 'male' ? 'Мужской' : 'Женский'}
• Возраст: ${userInfo.age} лет
• Группа: ${userInfo.group}

📈 <b>Результаты:</b>`;

        // Добавляем результаты в зависимости от типа теста
        if (testName === 'САН') {
            message += `
• Самочувствие: ${results.wellbeing} баллов
• Активность: ${results.activity} баллов
• Настроение: ${results.mood} баллов
• Общий балл: ${results.total} баллов`;
        } else if (testName === 'PSM-25') {
            message += `
• Общий балл стресса: ${results.total} баллов
• Средний уровень стресса: ${results.level} из 8`;
        } else if (testName === 'Опросник выгорания') {
            message += `
• Эмоциональное истощение: ${results.emotional} баллов (${results.emotionalLevel})
• Деперсонализация: ${results.depersonalization} баллов (${results.depersonalizationLevel})
• Редукция профессионализма: ${results.professional} баллов (${results.professionalLevel})`;
        } else if (testName === 'Шкала тревоги') {
            message += `
• Ситуационная тревожность: ${results.state} баллов (средний: ${results.stateAverage.toFixed(1)}) - ${results.stateLevel}
• Личностная тревожность: ${results.trait} баллов (средний: ${results.traitAverage.toFixed(1)}) - ${results.traitLevel}`;
        } else if (testName === 'Шкала депрессии') {
            message += `
• Общий балл: ${results.total} баллов - ${results.level}
• Когнитивно-аффективная субшкала: ${results.cognitiveAffective} баллов
• Соматическая субшкала: ${results.somatic} баллов`;
        } else if (testName === 'Опросник риска буллинга') {
            message += `
• Шкала небезопасности: ${results.insecurity} баллов - ${results.insecurityLevel}
• Шкала благополучия: ${results.wellbeing} баллов - ${results.wellbeingLevel}
• Шкала разобщенности: ${results.disunity} баллов - ${results.disunityLevel}
• Шкала равноправия: ${results.equality} баллов - ${results.equalityLevel}`;
        } else if (testName === 'Опросник Д. Олвеуса «Буллинг»') {
            message += `
• Активный буллинг: ${results.activeBullying} баллов - ${results.activeBullyingLevel}
• Прямой активный буллинг: ${results.directActive} баллов - ${results.directActiveLevel}
• Косвенный активный буллинг: ${results.indirectActive} баллов - ${results.indirectActiveLevel}
• Пассивный буллинг (виктимизация): ${results.passiveBullying} баллов - ${results.passiveBullyingLevel}
• Прямая виктимизация: ${results.directVictim} баллов - ${results.directVictimLevel}
• Косвенная виктимизация: ${results.indirectVictim} баллов - ${results.indirectVictimLevel}`;
        } else if (testName === 'Диагностика агрессивного поведения') {
            message += `
• Вербальная агрессия (ВА): ${results.verbal} баллов
• Физическая агрессия (ФА): ${results.physical} баллов
• Предметная агрессия (ПА): ${results.object} баллов
• Эмоциональная агрессия (ЭА): ${results.emotional} баллов
• Самоагрессия (СА): ${results.self} баллов
• Общий уровень агрессивности: ${results.total} баллов`;
        }

        message += `

⏰ Время прохождения: ${new Date().toLocaleString('ru-RU')}
🔗 Источник: Telegram WebApp`;

        // Отправляем сообщение всем админам
        const sendPromises = CONFIG.ADMIN_CHAT_IDS.map(async (adminId) => {
            const data = {
                chat_id: adminId,
                text: message,
                parse_mode: 'HTML'
            };
            return await sendToTelegram(data);
        });

        try {
            const responses = await Promise.all(sendPromises);
            const successCount = responses.filter(response => response !== null).length;
            console.log(`Результаты успешно отправлены ${successCount} из ${CONFIG.ADMIN_CHAT_IDS.length} админов в Telegram`);
        } catch (error) {
            console.error('Ошибка при отправке результатов админам:', error);
        }
    } catch (error) {
        console.error('Ошибка при отправке результатов админу:', error);
    }
}

// Данные для теста PSM-25
const psm25TestData = {
    name: "PSM-25 (Шкала психологического стресса)",
    questions: [
        { id: 1, text: "Я напряжен(а) и взволнован(а) (взвинчен)" },
        { id: 2, text: "У меня ком в горле, и (или) я ощущаю сухость во рту" },
        { id: 3, text: "Я перегружен(а) работой. Мне совсем не хватает времени" },
        { id: 4, text: "Я проглатываю пищу или забываю поесть" },
        { id: 5, text: "После работы я не могу отключиться от мыслей о незавершенных делах, проблемах, планах; я «застреваю» на переживаниях рабочих ситуаций и нерешенных вопросов, обдумываю свои идеи снова и снова" },
        { id: 6, text: "Я чувствую себя одиноким(ой), изолированным(ой) и непонятым(ой)" },
        { id: 7, text: "Я страдаю от физического недомогания; у меня болит голова, напряжены мышцы шеи, боли в спине, спазмы в желудке" },
        { id: 8, text: "Я поглощен(а) мрачными мыслями, измучен(а) тревожными состояниями" },
        { id: 9, text: "Меня внезапно бросает то в жар, то в холод" },
        { id: 10, text: "Я чувствую, что не справляюсь со всеми делами, которые должен(а) делать" },
        { id: 11, text: "Я легко могу заплакать" },
        { id: 12, text: "Я чувствую себя уставшим человеком" },
        { id: 13, text: "В трудной ситуации я крепко стискиваю зубы (или сжимаю кулаки)" },
        { id: 14, text: "Я не спокоен / не спокойна" },
        { id: 15, text: "Мне тяжело дышать, и (или) у меня внезапно перехватывает дыхание" },
        { id: 16, text: "Я имею проблемы с пищеварением и с кишечником (боли, колики, расстройства или запоры)" },
        { id: 17, text: "Я взволнован(а), обеспокоен(а) или смущен(а)" },
        { id: 18, text: "Я легко пугаюсь; шум или грохот заставляет меня вздрагивать" },
        { id: 19, text: "Мне необходимо более чем полчаса для того, чтобы заснуть" },
        { id: 20, text: "Я сбит(а) с толку; мои мысли спутаны; мне не хватает сосредоточенности, и я не могу сконцентрировать внимание" },
        { id: 21, text: "У меня усталый вид; мешки или круги под глазами" },
        { id: 22, text: "Я чувствую тяжесть на своих плечах" },
        { id: 23, text: "Я встревожен(а). Мне необходимо постоянно двигаться; я не могу устоять на одном месте" },
        { id: 24, text: "Мне трудно контролировать свои поступки, эмоции, настроение или жесты" },
        { id: 25, text: "Я чувствую напряженность" }
    ]
};

// Данные для теста выгорания
const burnoutTestData = {
    name: "Опросник выгорания",
    questions: [
        { id: 1, text: "Я чувствую себя эмоционально опустошенным", scale: "emotional" },
        { id: 2, text: "После работы я чувствую себя как «выжатый лимон»", scale: "emotional" },
        { id: 3, text: "Утром я чувствую усталость и нежелание идти на работу", scale: "emotional" },
        { id: 4, text: "Я хорошо понимаю, что чувствуют мои подчиненные и коллеги, и стараюсь учитывать это в интересах дела", scale: "professional", reverse: true },
        { id: 5, text: "Я чувствую, что общаюсь с некоторыми подчиненными и коллегами как с предметами (без теплоты и расположения к ним)", scale: "depersonalization" },
        { id: 6, text: "После работы на некоторое время хочется уединиться от всех и всего", scale: "emotional", reverse: true },
        { id: 7, text: "Я умею находить правильное решение в конфликтных ситуациях, возникающих при общении с коллегами", scale: "professional" },
        { id: 8, text: "Я чувствую угнетенность и апатию", scale: "emotional" },
        { id: 9, text: "Я уверен, что моя работа нужна людям", scale: "professional" },
        { id: 10, text: "В последнее время я стал более «черствым» по отношению к тем, с кем работаю", scale: "depersonalization" },
        { id: 11, text: "Я замечаю, что моя работа ожесточает меня", scale: "depersonalization" },
        { id: 12, text: "У меня много планов на будущее, и я верю в их осуществление", scale: "professional" },
        { id: 13, text: "Моя работа все больше меня разочаровывает", scale: "emotional" },
        { id: 14, text: "Мне кажется, что я слишком много работаю", scale: "emotional" },
        { id: 15, text: "Бывает, что мне действительно безразлично то, что происходит с некоторыми моими подчиненными и коллегами", scale: "depersonalization" },
        { id: 16, text: "Мне хочется уединиться и отдохнуть от всего и всех", scale: "emotional" },
        { id: 17, text: "Я легко могу создать атмосферу доброжелательности и сотрудничества в коллективе", scale: "professional" },
        { id: 18, text: "Во время работы я чувствую приятное оживление", scale: "professional" },
        { id: 19, text: "Благодаря своей работе я уже сделал в жизни много действительно ценного", scale: "professional" },
        { id: 20, text: "Я чувствую равнодушие и потерю интереса ко многому, что радовало меня в моей работе", scale: "emotional" },
        { id: 21, text: "На работе я спокойно справляюсь с эмоциональными проблемами", scale: "professional" },
        { id: 22, text: "В последнее время мне кажется, что коллеги и подчиненные все чаще перекладывают на меня груз своих проблем и обязанностей", scale: "depersonalization" }
    ]
};

// Данные для теста тревоги (Spielberger-Khanin)
const anxietyTestData = {
    name: "Шкала тревоги",
    stateQuestions: [ // Ситуационная тревожность (1-20)
        { id: 1, text: "Я спокоен", reverse: true },
        { id: 2, text: "Мне ничто не угрожает", reverse: true },
        { id: 3, text: "Я нахожусь в состоянии напряжения" },
        { id: 4, text: "Я внутренне спокоен", reverse: true },
        { id: 5, text: "Я чувствую себя спокойно", reverse: true },
        { id: 6, text: "Я расстроен" },
        { id: 7, text: "Меня волнуют возможные неудачи" },
        { id: 8, text: "Я ощущаю душевный покой", reverse: true },
        { id: 9, text: "Я встревожен" },
        { id: 10, text: "Я испытываю чувство внутреннего удовлетворения", reverse: true },
        { id: 11, text: "Я уверен в себе", reverse: true },
        { id: 12, text: "Я нервничаю" },
        { id: 13, text: "Я не нахожу себе места" },
        { id: 14, text: "Я взвинчен" },
        { id: 15, text: "Я не чувствую скованности и напряжения", reverse: true },
        { id: 16, text: "Я доволен", reverse: true },
        { id: 17, text: "Я озабочен" },
        { id: 18, text: "Я слишком возбужден, и мне не по себе" },
        { id: 19, text: "Мне радостно", reverse: true },
        { id: 20, text: "Мне приятно", reverse: true }
    ],
    traitQuestions: [ // Личностная тревожность (21-40)
        { id: 21, text: "У меня бывает приподнятое настроение", reverse: true },
        { id: 22, text: "Я бываю раздражительным" },
        { id: 23, text: "Я легко могу расстроиться" },
        { id: 24, text: "Я хотел бы быть таким же удачливым, как другие" },
        { id: 25, text: "Я сильно переживаю неприятности и долго не могу о них забыть" },
        { id: 26, text: "Я чувствую прилив сил, желание работать", reverse: true },
        { id: 27, text: "Я спокоен, хладнокровен и собран", reverse: true },
        { id: 28, text: "Меня тревожат возможные трудности" },
        { id: 29, text: "Я слишком переживаю из-за пустяков" },
        { id: 30, text: "Я бываю вполне счастлив", reverse: true },
        { id: 31, text: "Я все принимаю близко к сердцу" },
        { id: 32, text: "Мне не хватает уверенности к себе" },
        { id: 33, text: "Я чувствую себя беззащитным" },
        { id: 34, text: "Я стараюсь избегать критических ситуаций и трудностей" },
        { id: 35, text: "У меня бывает хандра" },
        { id: 36, text: "Я бываю доволен", reverse: true },
        { id: 37, text: "Всякие пустяки отвлекают и волнуют меня" },
        { id: 38, text: "Бывает, что я чувствую себя неудачником" },
        { id: 39, text: "Я уверенный человек", reverse: true },
        { id: 40, text: "Меня охватывает беспокойство, когда я думаю о своих делах и заботах" }
    ]
};

// Данные для теста Шкала депрессии Бека
const beckDepressionTestData = {
    name: "Шкала депрессии Бека",
    questions: [
        { id: 1, text: "Я не чувствую себя расстроенным, печальным", score: 0 },
        { id: 1, text: "Я расстроен", score: 1 },
        { id: 1, text: "Я все время расстроен и не могу от этого отключиться", score: 2 },
        { id: 1, text: "Я настолько расстроен и несчастлив, что не могу это выдержать", score: 3 },
        
        { id: 2, text: "Я не тревожусь о своем будущем", score: 0 },
        { id: 2, text: "Я чувствую, что озадачен будущим", score: 1 },
        { id: 2, text: "Я чувствую, что меня ничего не ждет в будущем", score: 2 },
        { id: 2, text: "Мое будущее безнадежно, и ничто не может измениться к лучшему", score: 3 },
        
        { id: 3, text: "Я не чувствую себя неудачником", score: 0 },
        { id: 3, text: "Я чувствую, что терпел больше неудач, чем другие люди", score: 1 },
        { id: 3, text: "Когда я оглядываюсь на свою жизнь, я вижу в ней много неудач", score: 2 },
        { id: 3, text: "Я чувствую, что как личность я - полный неудачник", score: 3 },
        
        { id: 4, text: "Я получаю столько же удовлетворения от жизни, как раньше", score: 0 },
        { id: 4, text: "Я не получаю столько же удовлетворения от жизни, как раньше", score: 1 },
        { id: 4, text: "Я больше не получаю удовлетворения ни от чего", score: 2 },
        { id: 4, text: "Я полностью не удовлетворен жизнью и мне все надоело", score: 3 },
        
        { id: 5, text: "Я не чувствую себя в чем-нибудь виноватым", score: 0 },
        { id: 5, text: "Достаточно часто я чувствую себя виноватым", score: 1 },
        { id: 5, text: "Большую часть времени я чувствую себя виноватым", score: 2 },
        { id: 5, text: "Я постоянно испытываю чувство вины", score: 3 },
        
        { id: 6, text: "Я не чувствую, что могу быть наказанным за что-либо", score: 0 },
        { id: 6, text: "Я чувствую, что могу быть наказан", score: 1 },
        { id: 6, text: "Я ожидаю, что могу быть наказан", score: 2 },
        { id: 6, text: "Я чувствую себя уже наказанным", score: 3 },
        
        { id: 7, text: "Я не разочаровался в себе", score: 0 },
        { id: 7, text: "Я разочаровался в себе", score: 1 },
        
        { id: 8, text: "Я не чувствую себя хуже других", score: 0 },
        { id: 8, text: "Я критикую себя за слабости и ошибки", score: 1 },
        { id: 8, text: "Я виню себя за все плохое, что происходит", score: 2 },
        { id: 8, text: "Я виню себя во всем плохом, что происходит", score: 3 },
        
        { id: 9, text: "Я не думаю о том, чтобы покончить с собой", score: 0 },
        { id: 9, text: "У меня есть мысли покончить с собой, но я не буду этого делать", score: 1 },
        { id: 9, text: "Я хотел бы покончить с собой", score: 2 },
        { id: 9, text: "Я бы покончил с собой, если бы представилась возможность", score: 3 },
        
        { id: 10, text: "Я плачу не больше обычного", score: 0 },
        { id: 10, text: "Сейчас я плачу больше, чем раньше", score: 1 },
        { id: 10, text: "Сейчас я все время плачу", score: 2 },
        { id: 10, text: "Раньше я мог плакать, но теперь не могу, даже если хочется", score: 3 },
        
        { id: 11, text: "Сейчас я не более раздражителен, чем обычно", score: 0 },
        { id: 11, text: "Я более легко раздражаюсь, чем раньше", score: 1 },
        { id: 11, text: "Сейчас я постоянно чувствую раздражение", score: 2 },
        { id: 11, text: "Я стал равнодушен к вещам, которые раньше меня раздражали", score: 3 },
        
        { id: 12, text: "Я не потерял интереса к другим людям", score: 0 },
        { id: 12, text: "Я меньше интересуюсь другими людьми, чем раньше", score: 1 },
        { id: 12, text: "Я потерял большую часть интереса к другим людям", score: 2 },
        { id: 12, text: "Я потерял всякий интерес к другим людям", score: 3 },
        
        { id: 13, text: "Я принимаю решения так же хорошо, как и раньше", score: 0 },
        { id: 13, text: "Я откладываю принятие решений на потом", score: 1 },
        { id: 13, text: "Мне труднее принимать решения, чем раньше", score: 2 },
        { id: 13, text: "Я больше не могу принимать решения", score: 3 },
        
        { id: 14, text: "Я не чувствую, что выгляжу хуже, чем обычно", score: 0 },
        { id: 14, text: "Меня беспокоит, что я выгляжу старым или непривлекательным", score: 1 },
        { id: 14, text: "Я знаю, что в моей внешности произошли постоянные изменения, которые делают меня непривлекательным", score: 2 },
        { id: 14, text: "Я знаю, что выгляжу непривлекательно", score: 3 },
        
        { id: 15, text: "Я могу работать так же хорошо, как и раньше", score: 0 },
        { id: 15, text: "Мне требуется дополнительное усилие, чтобы начать что-то делать", score: 1 },
        { id: 15, text: "Я с трудом заставляю себя делать что-либо", score: 2 },
        { id: 15, text: "Я совсем не могу выполнять никакую работу", score: 3 },
        
        { id: 16, text: "Я сплю так же хорошо, как и раньше", score: 0 },
        { id: 16, text: "Сейчас я сплю хуже, чем раньше", score: 1 },
        { id: 16, text: "Я просыпаюсь на 1-2 часа раньше обычного и с трудом засыпаю снова", score: 2 },
        { id: 16, text: "Я просыпаюсь на несколько часов раньше обычного и больше не могу заснуть", score: 3 },
        
        { id: 17, text: "Я устаю не больше, чем обычно", score: 0 },
        { id: 17, text: "Теперь я устаю быстрее, чем раньше", score: 1 },
        { id: 17, text: "Я устаю почти от всего, что я делаю", score: 2 },
        { id: 17, text: "Я не могу ничего делать из-за усталости", score: 3 },
        
        { id: 18, text: "Мой аппетит не хуже, чем обычно", score: 0 },
        { id: 18, text: "Мой аппетит стал хуже, чем раньше", score: 1 },
        { id: 18, text: "Мой аппетит теперь значительно хуже", score: 2 },
        { id: 18, text: "У меня вообще нет аппетита", score: 3 },
        
        { id: 19, text: "В последнее время я не похудел или потеря веса была незначительной", score: 0 },
        { id: 19, text: "За последнее время я потерял более 2 кг", score: 1 },
        { id: 19, text: "Я потерял более 5 кг", score: 2 },
        { id: 19, text: "Я потерял более 7 кг", score: 3 },
        
        { id: 20, text: "Я беспокоюсь о своем здоровье не больше, чем обычно", score: 0 },
        { id: 20, text: "Меня беспокоят проблемы физического здоровья, такие как боли, расстройство желудка, запоры и т.д.", score: 1 },
        { id: 20, text: "Я очень обеспокоен своим физическим состоянием, и мне трудно думать о чем-то другом", score: 2 },
        { id: 20, text: "Я настолько обеспокоен своим физическим состоянием, что больше ни о чем не могу думать", score: 3 },
        
        { id: 21, text: "В последнее время я не замечал изменения своего интереса к сексу", score: 0 },
        { id: 21, text: "Меня меньше интересует секс, чем раньше", score: 1 },
        { id: 21, text: "Сейчас я значительно меньше интересуюсь сексом", score: 2 },
        { id: 21, text: "Я полностью потерял интерес к сексу", score: 3 }
    ]
};

// Данные для теста на буллинг
const bullyingTestData = {
    name: "Опросник риска буллинга",
    description: "Оценка атмосферы в университете и риска буллинга",
    sections: [
        {
            id: 1,
            title: "В вашей группе принято...",
            questions: [
                { id: "1.1", text: "мешать друг другу, лезть, приставать", scale: "insecurity" },
                { id: "1.2", text: "вместе развлекаться после пар", scale: "wellbeing", reverse: true },
                { id: "1.3", text: "шутить над кем-нибудь так, чтобы смеялась вся группа", scale: "insecurity" },
                { id: "1.4", text: "драться", scale: "disunity" },
                { id: "1.5", text: "ходить друг к другу в гости", scale: "wellbeing", reverse: true },
                { id: "1.6", text: "заступаться за своих", scale: "wellbeing", reverse: true },
                { id: "1.7", text: "обзываться", scale: "insecurity" },
                { id: "1.8", text: "не мешать друг другу заниматься, чем захочется", scale: "insecurity", reverse: true }
            ]
        },
        {
            id: 2,
            title: "В вашей группе есть кто-то...",
            questions: [
                { id: "2.1", text: "кого все уважают", scale: "equality" },
                { id: "2.2", text: "кого все боятся", scale: "equality" },
                { id: "2.3", text: "над кем все смеются", scale: "insecurity" },
                { id: "2.4", text: "кем часто недовольны преподаватели", scale: "insecurity" },
                { id: "2.5", text: "на кого хочется быть похожим", scale: "equality" },
                { id: "2.6", text: "с кем лучше не спорить", scale: "equality" },
                { id: "2.7", text: "кто никогда не прогуливает", scale: "wellbeing" },
                { id: "2.8", text: "с кем даже преподаватель не может справиться", scale: "insecurity" }
            ]
        },
        {
            id: 3,
            title: "Как к вам в группе обращаются обычно преподаватели?",
            questions: [
                { id: "3.1", text: "по имени", scale: "wellbeing" },
                { id: "3.2", text: "по имени и отчеству", scale: "equality" },
                { id: "3.3", text: "по фамилии", scale: "wellbeing", reverse: true },
                { id: "3.4", text: "по прозвищам", scale: "insecurity" }
            ]
        },
        {
            id: 4,
            title: "Когда в университете происходит драка, вы...",
            questions: [
                { id: "4.1", text: "удивляетесь", scale: "insecurity", reverse: true },
                { id: "4.2", text: "не обращаете внимания, это обычное дело", scale: "disunity" },
                { id: "4.3", text: "присоединяетесь, встав на чью-то сторону", scale: "equality" },
                { id: "4.4", text: "много это потом обсуждаете между собой в группе", scale: "equality" }
            ]
        },
        {
            id: 5,
            title: "Ценные вещи...",
            questions: [
                { id: "5.1", text: "стараюсь не носить в университет вообще", scale: "wellbeing", reverse: true },
                { id: "5.2", text: "спокойно оставляю в аудитории", scale: "wellbeing" },
                { id: "5.3", text: "можно оставить в коридоре", scale: "wellbeing" },
                { id: "5.4", text: "был случай, что украли", scale: "wellbeing", reverse: true },
                { id: "5.5", text: "оставляю в раздевалке", scale: "equality" }
            ]
        },
        {
            id: 6,
            title: "Вызов в деканат - это...",
            questions: [
                { id: "6.1", text: "хотят за что-то похвалить", scale: "equality" }
            ]
        },
        {
            id: 7,
            title: "В вашем университете мат, ругательства...",
            questions: [
                { id: "7.1", text: "звучат на переменах в личных разговорах", scale: "insecurity" },
                { id: "7.2", text: "не приняты вообще", scale: "insecurity", reverse: true }
            ]
        },
        {
            id: 8,
            title: "В вашем университете...",
            questions: [
                { id: "8.1", text: "курят в туалетах, под лестницами", scale: "insecurity" }
            ]
        },
        {
            id: 9,
            title: "В вашем университете стены, мебель...",
            questions: [
                { id: "9.1", text: "исписанные, испачканные", scale: "insecurity" }
            ]
        },
        {
            id: 10,
            title: "Если кто-то начинает орать, драться, класс «встает на уши»; что нужно, чтобы это прекратилось:",
            questions: [
                { id: "10.1", text: "кто-то из студентов должен сказать «хватит»", scale: "insecurity", reverse: true },
                { id: "10.2", text: "должен прийти куратор", scale: "insecurity" },
                { id: "10.3", text: "это прекратится, когда все устанут", scale: "disunity" }
            ]
        },
        {
            id: 11,
            title: "В университете вам...",
            questions: [
                { id: "11.1", text: "в целом нравится, приятно, интересно", scale: "wellbeing", reverse: true },
                { id: "11.2", text: "в целом не нравится, плохо, никто ни с кем не дружит", scale: "disunity" }
            ]
        },
        {
            id: 12,
            title: "Перемену я провожу:",
            questions: [
                { id: "12.1", text: "захожу к друзьям в другие аудитории", scale: "equality" }
            ]
        },
        {
            id: 13,
            title: "Когда ваша группа едет куда-то с преподавателями:",
            questions: [
                { id: "13.1", text: "это обычная ситуация", scale: "wellbeing" },
                { id: "13.2", text: "вам это нравится, это весело", scale: "wellbeing", reverse: true },
                { id: "13.3", text: "вы стараетесь не ездить", scale: "disunity" },
                { id: "13.4", text: "преподавателям это сложно, они каждый говорят, что это «в последний раз»", scale: "wellbeing", reverse: true }
            ]
        },
        {
            id: 14,
            title: "Ваша группа имеет репутацию",
            questions: [
                { id: "14.1", text: "отличников", scale: "equality" },
                { id: "14.2", text: "хулиганов", scale: "insecurity" },
                { id: "14.3", text: "самой обычной, ничем не отличающейся от других групп в университете", scale: "wellbeing" },
                { id: "14.4", text: "группа, в которой никто не хочет быть куратором", scale: "wellbeing", reverse: true }
            ]
        }
    ],
    scales: {
        insecurity: {
            name: "Шкала небезопасности",
            description: "Характеризует распространенность неуважения, небезопасности и пренебрежения правилами и границами в классе"
        },
        wellbeing: {
            name: "Шкала благополучия", 
            description: "Характеризует стабильность границ, соблюдение правил и установление уважения как нормы в группе"
        },
        disunity: {
            name: "Шкала разобщенности",
            description: "Выявляет отсутствие сплоченности, степень дистанции между подростками и между подростками и учителями"
        },
        equality: {
            name: "Шкала равноправия",
            description: "Оценивает способность группы принимать различия между участниками, распределять роли и вступать в конструктивное общение"
        }
    }
};

// Данные для теста Д. Олвеуса "Буллинг"
const olweusBullyingTestData = {
    name: "Опросник Д. Олвеуса «Буллинг»",
    description: "Выявление проявлений буллинга и подверженности ему",
    questions: [
        // Активный буллинг (проявление агрессии)
        // Прямой активный буллинг (вопросы 1, 3, 5, 6)
        { id: 1, text: "я кого-то обозвал", scale: "direct_active" },
        { id: 2, text: "я с кем-то специально не разговаривал", scale: "indirect_active" },
        { id: 3, text: "я нанес кому-то физический вред, например, толкнул или ударил", scale: "direct_active" },
        { id: 4, text: "я распространял о ком-то сплетни", scale: "indirect_active" },
        { id: 5, text: "я угрожал", scale: "direct_active" },
        { id: 6, text: "я украл или испортил чьи-то вещи", scale: "direct_active" },
        
        // Пассивный буллинг (виктимизация)
        // Прямая виктимизация (вопросы 7, 10, 11, 13)
        { id: 7, text: "меня обзывали", scale: "direct_victim" },
        { id: 8, text: "обо мне распространяли сплетни", scale: "indirect_victim" },
        { id: 9, text: "никто не хочет сидеть со мной или проводить свободное время", scale: "indirect_victim" },
        { id: 10, text: "у меня украли вещи", scale: "direct_victim" },
        { id: 11, text: "мне нанесли физический вред (ударили, толкнули)", scale: "direct_victim" },
        { id: 12, text: "никто не говорит со мной", scale: "indirect_victim" },
        { id: 13, text: "мне угрожали", scale: "direct_victim" }
    ],
    scales: {
        direct_active: {
            name: "Прямой активный буллинг",
            description: "Проявления физической и вербальной агрессии",
            questions: [1, 3, 5, 6]
        },
        indirect_active: {
            name: "Косвенный активный буллинг", 
            description: "Проявления изоляции (социальной депривации)",
            questions: [2, 4]
        },
        direct_victim: {
            name: "Прямая виктимизация",
            description: "Подверженность физической и вербальной агрессии",
            questions: [7, 10, 11, 13]
        },
        indirect_victim: {
            name: "Косвенная виктимизация",
            description: "Подверженность социальной депривации",
            questions: [8, 9, 12]
        }
    }
};

// Данные для теста агрессивного поведения (Л.Г. Почебут)
const aggressionTestData = {
    name: "Диагностика агрессивного поведения",
    description: "Опросник Л.Г. Почебут для оценки агрессивного поведения",
    questions: [
        { id: 1, text: "Во время спора я часто повышаю голос." },
        { id: 2, text: "Если меня кто-то раздражает, я могу сказать ему все, что о нем думаю." },
        { id: 3, text: "Если мне необходимо будет прибегнуть к физической силе для защиты своих прав, я, не раздумывая, сделаю это." },
        { id: 4, text: "Когда я встречаю неприятного мне человека, я могу позволить себе незаметно ущипнуть или толкнуть его." },
        { id: 5, text: "Увлекшись спором с другим человеком, я могу стукнуть кулаком по столу, чтобы привлечь к себе внимание или доказать свою правоту." },
        { id: 6, text: "Я постоянно чувствую, что другие не уважают мои права." },
        { id: 7, text: "Вспоминая прошлое, порой мне бывает обидно за себя." },
        { id: 8, text: "Хотя я и не подаю вида, иногда меня гложет зависть." },
        { id: 9, text: "Если я не одобряю поведение своих знакомых, то я прямо говорю им об этом." },
        { id: 10, text: "В сильном гневе я употребляю крепкие выражения, сквернословлю." },
        { id: 11, text: "Если кто-нибудь поднимет на меня руку, я постараюсь ударить его первым." },
        { id: 12, text: "Я бываю настолько взбешен, что швыряю разные предметы." },
        { id: 13, text: "У меня часто возникает потребность переставить в квартире мебель или полностью сменить ее." },
        { id: 14, text: "В общении с людьми я часто чувствую себя «пороховой бочкой», которая постоянно готова взорваться." },
        { id: 15, text: "Порой у меня появляется желание зло пошутить над другим человеком." },
        { id: 16, text: "Когда я сердит, то обычно мрачнею." },
        { id: 17, text: "В разговоре с человеком я стараюсь его внимательно выслушать, не перебивая." },
        { id: 18, text: "Пару лет назад у меня часто «чесались кулаки» и я всегда был готов пустить их в ход." },
        { id: 19, text: "Если я знаю, что человек намеренно меня толкнул, то дело может дойти до драки." },
        { id: 20, text: "Творческий беспорядок на моем рабочем столе позволяет мне эффективно работать." },
        { id: 21, text: "Я помню, что бывал настолько сердитым, что хватал все, что попадало под руку, и ломал." },
        { id: 22, text: "Иногда люди раздражают меня только одним своим присутствием." },
        { id: 23, text: "Я часто удивляюсь, какие скрытые причины заставляют другого человека делать мне что-нибудь хорошее." },
        { id: 24, text: "Если мне нанесут обиду, у меня пропадет желание разговаривать с кем бы то ни было." },
        { id: 25, text: "Иногда я намеренно говорю гадости о человеке, которого не люблю." },
        { id: 26, text: "Когда я взбешен, я кричу самое злобное ругательство." },
        { id: 27, text: "В детстве я избегал драться." },
        { id: 28, text: "Я знаю, по какой причине и когда можно кого-нибудь ударить." },
        { id: 29, text: "Когда я взбешен, то могу хлопнуть дверью." },
        { id: 30, text: "Мне кажется, что окружающие люди меня не любят." },
        { id: 31, text: "Я постоянно делюсь с другими своими чувствами и переживаниями." },
        { id: 32, text: "Очень часто своими словами и действиями я сам себе приношу вред." },
        { id: 33, text: "Когда люди орут на меня, я отвечаю тем же." },
        { id: 34, text: "Если кто-нибудь ударит меня первым, я в ответ ударю его." },
        { id: 35, text: "Меня раздражает, когда предметы лежат не на своем месте." },
        { id: 36, text: "Если мне не удается починить сломавшийся или порвавшийся предмет, то я в гневе ломаю или рву его окончательно." },
        { id: 37, text: "Другие люди мне всегда кажутся преуспевающими." },
        { id: 38, text: "Когда я думаю об очень неприятном мне человеке, я могу прийти в возбуждение от желания причинить ему зло." },
        { id: 39, text: "Иногда мне кажется, что судьба сыграла со мной злую шутку." },
        { id: 40, text: "Если кто-нибудь обращается со мной не так, как следует, я очень расстраиваюсь по этому поводу." }
    ],
    // Ключ для обработки результатов
    scoringKey: {
        verbal: {
            name: "Вербальная агрессия (ВА)",
            description: "Выражение агрессии вербально, использование оскорблений",
            yes: [1, 2, 9, 10, 25, 26, 33],
            no: [17]
        },
        physical: {
            name: "Физическая агрессия (ФА)", 
            description: "Выражение агрессии с использованием физической силы",
            yes: [3, 4, 11, 18, 19, 28, 34],
            no: [27]
        },
        object: {
            name: "Предметная агрессия (ПА)",
            description: "Направление агрессии на окружающие предметы",
            yes: [5, 12, 13, 21, 29, 35, 36],
            no: [20]
        },
        emotional: {
            name: "Эмоциональная агрессия (ЭА)",
            description: "Эмоциональное отчуждение, подозрительность, враждебность",
            yes: [6, 14, 15, 22, 30, 37, 38],
            no: [23]
        },
        self: {
            name: "Самоагрессия (СА)",
            description: "Отсутствие внутреннего покоя, ослабленные защитные механизмы",
            yes: [7, 8, 16, 24, 32, 39, 40],
            no: [31]
        }
    }
};

// Данные для теста САН
const sanTestData = {
    name: "САН (Самочувствие - Активность - Настроение)",
    questions: [
        { id: 1, left: "Самочувствие хорошее", right: "Самочувствие плохое", category: "wellbeing", reverse: false },
        { id: 2, left: "Чувствую себя сильным", right: "Чувствую себя слабым", category: "wellbeing", reverse: false },
        { id: 3, left: "Пассивный", right: "Активный", category: "activity", reverse: true },
        { id: 4, left: "Малоподвижный", right: "Подвижный", category: "activity", reverse: true },
        { id: 5, left: "Веселый", right: "Грустный", category: "mood", reverse: false },
        { id: 6, left: "Хорошее настроение", right: "Плохое настроение", category: "mood", reverse: false },
        { id: 7, left: "Работоспособный", right: "Разбитый", category: "wellbeing", reverse: false },
        { id: 8, left: "Полный сил", right: "Обессиленный", category: "wellbeing", reverse: false },
        { id: 9, left: "Медлительный", right: "Быстрый", category: "activity", reverse: true },
        { id: 10, left: "Бездеятельный", right: "Деятельный", category: "activity", reverse: true },
        { id: 11, left: "Счастливый", right: "Несчастный", category: "mood", reverse: false },
        { id: 12, left: "Жизнерадостный", right: "Мрачный", category: "mood", reverse: false },
        { id: 13, left: "Напряженный", right: "Расслабленный", category: "wellbeing", reverse: true },
        { id: 14, left: "Здоровый", right: "Больной", category: "wellbeing", reverse: false },
        { id: 15, left: "Безучастный", right: "Увлеченный", category: "activity", reverse: true },
        { id: 16, left: "Равнодушный", right: "Заинтересованный", category: "activity", reverse: true },
        { id: 17, left: "Восторженный", right: "Унылый", category: "mood", reverse: false },
        { id: 18, left: "Радостный", right: "Печальный", category: "mood", reverse: false },
        { id: 19, left: "Отдохнувший", right: "Усталый", category: "wellbeing", reverse: false },
        { id: 20, left: "Свежий", right: "Изнуренный", category: "wellbeing", reverse: false },
        { id: 21, left: "Сонливый", right: "Возбужденный", category: "activity", reverse: true },
        { id: 22, left: "Желание отдохнуть", right: "Желание работать", category: "activity", reverse: true },
        { id: 23, left: "Спокойный", right: "Взволнованный", category: "mood", reverse: true },
        { id: 24, left: "Оптимистичный", right: "Пессимистичный", category: "mood", reverse: false },
        { id: 25, left: "Выносливый", right: "Утомляемый", category: "wellbeing", reverse: false },
        { id: 26, left: "Бодрый", right: "Вялый", category: "wellbeing", reverse: false },
        { id: 27, left: "Соображать трудно", right: "Соображать легко", category: "activity", reverse: true },
        { id: 28, left: "Рассеянный", right: "Внимательный", category: "activity", reverse: true },
        { id: 29, left: "Полный надежд", right: "Разочарованный", category: "mood", reverse: false },
        { id: 30, left: "Довольный", right: "Недовольный", category: "mood", reverse: false }
    ]
};

// Инициализация приложения
document.addEventListener('DOMContentLoaded', function() {
    initializeApp();
});

function initializeApp() {
    // Обработчики событий
    document.getElementById('user-info-form').addEventListener('submit', handleUserInfoSubmit);
    document.getElementById('back-to-menu').addEventListener('click', showMainScreen);
    document.getElementById('restart-test').addEventListener('click', restartTest);
    
    // Обработчики для пунктов главного меню
    document.querySelectorAll('.menu-item').forEach(item => {
        item.addEventListener('click', handleMenuClick);
    });
    
    // Обработчики для тестов
    document.querySelectorAll('.test-card').forEach(card => {
        card.addEventListener('click', handleTestCardClick);
    });
    
    // Инициализация формы консультации
    initConsultationForm();
    
    // Обработчики навигации по тесту САН
    document.getElementById('next-question').addEventListener('click', nextQuestion);
    document.getElementById('prev-question').addEventListener('click', prevQuestion);
    document.getElementById('finish-test').addEventListener('click', finishTest);
    
    // Обработчики навигации по тесту PSM-25
    document.getElementById('psm25-next-question').addEventListener('click', nextPsm25Question);
    document.getElementById('psm25-prev-question').addEventListener('click', prevPsm25Question);
    document.getElementById('psm25-finish-test').addEventListener('click', finishPsm25Test);
    
    // Обработчики для результатов PSM-25
    document.getElementById('psm25-back-to-menu').addEventListener('click', showMainScreen);
    document.getElementById('psm25-restart-test').addEventListener('click', restartPsm25Test);
    
    // Обработчики для результатов теста выгорания
    document.getElementById('burnout-back-to-menu').addEventListener('click', showMainScreen);
    document.getElementById('burnout-restart-test').addEventListener('click', restartBurnoutTest);
    
    // Обработчики для результатов теста тревоги
    document.getElementById('anxiety-back-to-menu').addEventListener('click', showMainScreen);
    document.getElementById('anxiety-restart-test').addEventListener('click', restartAnxietyTest);
    
    // Обработчики для результатов теста депрессии
    document.getElementById('depression-back-to-menu').addEventListener('click', showMainScreen);
    document.getElementById('depression-restart-test').addEventListener('click', restartDepressionTest);
    
    // Загрузить сохраненные данные пользователя
    loadUserInfo();
    
    // Показать главный экран с вкладками
    showScreen('main-screen');
}

function showScreen(screenId) {
    document.querySelectorAll('.screen').forEach(screen => {
        screen.classList.remove('active');
    });
    document.getElementById(screenId).classList.add('active');
}

function showMainScreen() {
    showScreen('main-screen');
    updateTestStatuses();
}

function handleMenuClick(e) {
    const sectionId = e.currentTarget.dataset.section;
    
    // Показать соответствующую страницу
    showScreen(`${sectionId}-screen`);
}

function handleUserInfoSubmit(e) {
    e.preventDefault();
    
    // Получить значения полей
    const gender = document.getElementById('gender').value;
    const age = document.getElementById('age').value;
    const group = document.getElementById('group').value;
    
    // Дополнительная валидация
    if (!gender) {
        alert('Пожалуйста, выберите пол');
        return;
    }
    
    if (!age || age < 1 || age > 120) {
        alert('Пожалуйста, введите корректный возраст (от 1 до 120 лет)');
        return;
    }
    
    // Номер группы необязателен
    
    userInfo = {
        gender: gender,
        age: parseInt(age),
        group: group.trim() || 'Не указано'
    };
    
    // Сохранить данные пользователя
    localStorage.setItem('userInfo', JSON.stringify(userInfo));
    
    // Если был выбран тест, начать его
    if (currentTest === 'san') {
        startSanTest();
    } else if (currentTest === 'psm25') {
        startPsm25Test();
    } else if (currentTest === 'burnout') {
        startBurnoutTest();
    } else if (currentTest === 'anxiety') {
        startAnxietyTest();
    } else if (currentTest === 'depression') {
        startDepressionTest();
    } else if (currentTest === 'bullying') {
        startBullyingTest();
    } else if (currentTest === 'olweus-bullying') {
        startOlweusBullyingTest();
    } else if (currentTest === 'aggression') {
        startAggressionTest();
    } else {
        // Если тест не выбран, показать экран выбора тестов
        showTestsMenu();
    }
}

function loadUserInfo() {
    const savedUserInfo = localStorage.getItem('userInfo');
    if (savedUserInfo) {
        userInfo = JSON.parse(savedUserInfo);
    }
}

function fillUserInfoForm() {
    // Очистить форму перед заполнением
    document.getElementById('gender').value = '';
    document.getElementById('age').value = '';
    document.getElementById('group').value = '';
    
    // Заполнить форму данными пользователя
    if (userInfo.gender) {
        document.getElementById('gender').value = userInfo.gender;
    }
    if (userInfo.age) {
        document.getElementById('age').value = userInfo.age;
    }
    if (userInfo.group && userInfo.group !== 'Не указано') {
        document.getElementById('group').value = userInfo.group;
    }
}

function showTestsMenu() {
    showMainScreen();
}

function updateTestStatuses() {
    const completedTests = JSON.parse(localStorage.getItem('completedTests') || '{}');
    
    Object.keys(completedTests).forEach(testId => {
        const statusElement = document.getElementById(`${testId}-status`);
        if (statusElement) {
            statusElement.textContent = 'Пройден';
            statusElement.classList.add('completed');
        }
    });
}

function handleTestCardClick(e) {
    const card = e.currentTarget;
    if (card.classList.contains('disabled')) return;
    
    const testId = card.dataset.test;
    currentTest = testId;
    
    // Всегда показывать экран ввода данных с информацией о выбранном тесте
    showSelectedTestInfo(testId);
    showScreen('user-info-screen');
    fillUserInfoForm();
}

function showSelectedTestInfo(testId) {
    const testInfo = document.getElementById('selected-test-info');
    const testName = document.getElementById('test-name');
    const testDescription = document.getElementById('test-description');
    
    if (testId === 'san') {
        testName.textContent = 'САН';
        testDescription.textContent = 'Самочувствие - Активность - Настроение';
        testInfo.style.display = 'block';
    } else if (testId === 'psm25') {
        testName.textContent = 'PSM-25';
        testDescription.textContent = 'Шкала психологического стресса';
        testInfo.style.display = 'block';
    } else if (testId === 'burnout') {
        testName.textContent = 'Опросник выгорания';
        testDescription.textContent = 'Профессиональное выгорание';
        testInfo.style.display = 'block';
    } else if (testId === 'anxiety') {
        testName.textContent = 'Шкала тревоги';
        testDescription.textContent = 'Ситуационная и личностная тревожность';
        testInfo.style.display = 'block';
    } else if (testId === 'depression') {
        testName.textContent = 'Шкала депрессии';
        testDescription.textContent = 'Оценка депрессивных симптомов';
        testInfo.style.display = 'block';
    } else if (testId === 'bullying') {
        testName.textContent = 'Опросник риска буллинга';
        testDescription.textContent = 'Оценка атмосферы в школе и риска буллинга';
        testInfo.style.display = 'block';
    } else if (testId === 'olweus-bullying') {
        testName.textContent = 'Опросник Д. Олвеуса «Буллинг»';
        testDescription.textContent = 'Выявление проявлений буллинга и подверженности ему';
        testInfo.style.display = 'block';
    } else if (testId === 'aggression') {
        testName.textContent = 'Диагностика агрессивного поведения';
        testDescription.textContent = 'Опросник Л.Г. Почебут для оценки агрессивного поведения';
        testInfo.style.display = 'block';
    } else {
        testInfo.style.display = 'none';
    }
}

function startSanTest() {
    currentQuestion = 0;
    testAnswers = {};
    
    showScreen('san-test-screen');
    displayQuestion();
    updateProgress();
    updateNavigationButtons();
}

function displayQuestion() {
    const question = sanTestData.questions[currentQuestion];
    const container = document.getElementById('question-container');
    
    container.innerHTML = `
        <div class="question-item">
            <div class="question-number">Вопрос ${question.id}</div>
            <div class="question-pair">
                <div class="question-text">${question.left}</div>
                <div class="scale">
                    <div class="scale-option">
                        <input type="radio" name="question_${question.id}" value="3" id="q${question.id}_3">
                        <span class="scale-number">3</span>
                    </div>
                    <div class="scale-option">
                        <input type="radio" name="question_${question.id}" value="2" id="q${question.id}_2">
                        <span class="scale-number">2</span>
                    </div>
                    <div class="scale-option">
                        <input type="radio" name="question_${question.id}" value="1" id="q${question.id}_1">
                        <span class="scale-number">1</span>
                    </div>
                    <div class="scale-option">
                        <input type="radio" name="question_${question.id}" value="0" id="q${question.id}_0">
                        <span class="scale-number">0</span>
                    </div>
                    <div class="scale-option">
                        <input type="radio" name="question_${question.id}" value="1" id="q${question.id}_1r">
                        <span class="scale-number">1</span>
                    </div>
                    <div class="scale-option">
                        <input type="radio" name="question_${question.id}" value="2" id="q${question.id}_2r">
                        <span class="scale-number">2</span>
                    </div>
                    <div class="scale-option">
                        <input type="radio" name="question_${question.id}" value="3" id="q${question.id}_3r">
                        <span class="scale-number">3</span>
                    </div>
                </div>
                <div class="question-text">${question.right}</div>
            </div>
        </div>
    `;
    
    // Восстановить ответ если есть
    if (testAnswers[question.id] !== undefined) {
        const radio = document.querySelector(`input[name="question_${question.id}"][value="${testAnswers[question.id]}"]`);
        if (radio) radio.checked = true;
    }
    
    // Обработчики для радиокнопок
    document.querySelectorAll(`input[name="question_${question.id}"]`).forEach(radio => {
        radio.addEventListener('change', function() {
            testAnswers[question.id] = parseInt(this.value);
            updateNavigationButtons();
        });
    });
    
    // Обработчики клика для опций шкалы
    document.querySelectorAll(`.scale-option`).forEach(option => {
        option.addEventListener('click', function() {
            const radio = this.querySelector('input[type="radio"]');
            if (radio) {
                radio.checked = true;
                testAnswers[question.id] = parseInt(radio.value);
                updateNavigationButtons();
            }
        });
    });
    
    document.getElementById('current-question').textContent = currentQuestion + 1;
}

function updateProgress() {
    const progress = ((currentQuestion + 1) / sanTestData.questions.length) * 100;
    document.getElementById('san-progress').style.width = progress + '%';
}

function updateNavigationButtons() {
    const nextBtn = document.getElementById('next-question');
    const prevBtn = document.getElementById('prev-question');
    const finishBtn = document.getElementById('finish-test');
    
    prevBtn.disabled = currentQuestion === 0;
    
    if (currentQuestion === sanTestData.questions.length - 1) {
        nextBtn.style.display = 'none';
        finishBtn.style.display = 'inline-block';
        finishBtn.disabled = testAnswers[sanTestData.questions[currentQuestion].id] === undefined;
    } else {
        nextBtn.style.display = 'inline-block';
        finishBtn.style.display = 'none';
        nextBtn.disabled = testAnswers[sanTestData.questions[currentQuestion].id] === undefined;
    }
}

function nextQuestion() {
    if (currentQuestion < sanTestData.questions.length - 1) {
        currentQuestion++;
        displayQuestion();
        updateProgress();
        updateNavigationButtons();
    }
}

function prevQuestion() {
    if (currentQuestion > 0) {
        currentQuestion--;
        displayQuestion();
        updateProgress();
        updateNavigationButtons();
    }
}

function finishTest() {
    if (currentTest === 'san') {
        calculateSanResults();
    }
}

function calculateSanResults() {
    const results = {
        wellbeing: 0,
        activity: 0,
        mood: 0,
        total: 0
    };
    
    // Группировка вопросов по категориям
    const categories = {
        wellbeing: [1, 2, 7, 8, 13, 14, 19, 20, 25, 26],
        activity: [3, 4, 9, 10, 15, 16, 21, 22, 27, 28],
        mood: [5, 6, 11, 12, 17, 18, 23, 24, 29, 30]
    };
    
    // Подсчет баллов для каждой категории
    Object.keys(categories).forEach(category => {
        let sum = 0;
        categories[category].forEach(questionId => {
            const answer = testAnswers[questionId];
            const question = sanTestData.questions.find(q => q.id === questionId);
            
            if (answer !== undefined) {
                // Конвертация ответа в баллы от 1 до 7
                let score = answer + 4; // 0->4, 1->5, 2->6, 3->7
                
                // Обработка обратных вопросов
                if (question.reverse) {
                    score = 8 - score; // Инвертируем шкалу
                }
                
                sum += score;
            }
        });
        
        results[category] = (sum / categories[category].length).toFixed(1);
        results.total += sum;
    });
    
    results.total = (results.total / 30).toFixed(1);
    
    // Сохранение результатов
    const completedTests = JSON.parse(localStorage.getItem('completedTests') || '{}');
    completedTests.san = {
        results: results,
        timestamp: new Date().toISOString(),
        userInfo: userInfo
    };
    localStorage.setItem('completedTests', JSON.stringify(completedTests));
    
    // Отправка результатов в Telegram
    sendTestResultsToTelegram(results, userInfo, 'САН');
    
    // Отображение результатов
    displayResults(results);
}

function displayResults(results) {
    // Отображение баллов
    document.getElementById('wellbeing-score').textContent = results.wellbeing;
    document.getElementById('activity-score').textContent = results.activity;
    document.getElementById('mood-score').textContent = results.mood;
    document.getElementById('total-score').textContent = results.total;
    
    // Интерпретация результатов
    document.getElementById('wellbeing-interpretation').textContent = interpretScore(results.wellbeing, 'wellbeing');
    document.getElementById('activity-interpretation').textContent = interpretScore(results.activity, 'activity');
    document.getElementById('mood-interpretation').textContent = interpretScore(results.mood, 'mood');
    document.getElementById('total-interpretation').textContent = interpretTotalScore(results.total);
    
    // Дополнительная информация
    document.getElementById('summary-text').textContent = generateSummary(results);
    document.getElementById('recommendations-text').innerHTML = generateRecommendations(results);
    
    showScreen('results-screen');
}

function interpretScore(score, category) {
    const numScore = parseFloat(score);
    
    if (numScore >= 5.0 && numScore <= 5.5) {
        return getNormalInterpretation(category);
    } else if (numScore > 5.5) {
        return getFavorableInterpretation(category, numScore);
    } else {
        return getUnfavorableInterpretation(category, numScore);
    }
}

function getNormalInterpretation(category) {
    const interpretations = {
        wellbeing: "Нормальное самочувствие. Физиологическое и психологическое состояние в пределах нормы.",
        activity: "Нормальная активность. Умеренный уровень взаимодействия с окружающей средой.",
        mood: "Нормальное настроение. Стабильное эмоциональное состояние без выраженных колебаний."
    };
    return interpretations[category] || "Нормальное состояние";
}

function getFavorableInterpretation(category, score) {
    const interpretations = {
        wellbeing: `Отличное самочувствие (${score.toFixed(1)} баллов). Высокий уровень физического и психологического комфорта, бодрость, отсутствие недомоганий.`,
        activity: `Высокая активность (${score.toFixed(1)} баллов). Интенсивное взаимодействие с окружающей средой, инициативность, энергичность.`,
        mood: `Прекрасное настроение (${score.toFixed(1)} баллов). Приподнятый эмоциональный фон, позитивный настрой, жизнерадостность.`
    };
    return interpretations[category] || `Благоприятное состояние (${score.toFixed(1)} баллов)`;
}

function getUnfavorableInterpretation(category, score) {
    const interpretations = {
        wellbeing: `Сниженное самочувствие (${score.toFixed(1)} баллов). Возможны физические недомогания, усталость, дискомфорт. Рекомендуется отдых и внимание к здоровью.`,
        activity: `Низкая активность (${score.toFixed(1)} баллов). Сниженная инициативность, пассивность, возможно переутомление.`,
        mood: `Подавленное настроение (${score.toFixed(1)} баллов). Эмоциональный спад, возможны грусть, апатия. Рекомендуется поддержка и позитивные активности.`
    };
    return interpretations[category] || `Неблагоприятное состояние (${score.toFixed(1)} баллов)`;
}

function interpretTotalScore(score) {
    const numScore = parseFloat(score);
    
    if (numScore >= 1 && numScore <= 3) {
        return `Преобладает плохое настроение (${score} баллов). Общее состояние характеризуется негативным эмоциональным фоном, возможны депрессивные тенденции. Рекомендуется консультация специалиста.`;
    } else if (numScore >= 3.5 && numScore <= 4.5) {
        return `Изменчивое настроение (${score} баллов). Нестабильное эмоциональное состояние или затруднения в самооценке. Возможны перепады настроения, неопределенность в оценке собственного состояния.`;
    } else if (numScore >= 5 && numScore <= 7) {
        return `Преобладает хорошее настроение (${score} баллов). Позитивный эмоциональный фон, стабильное психологическое состояние, общий оптимизм и жизнерадостность.`;
    } else {
        return `Неопределенное состояние (${score} баллов). Результат требует дополнительной интерпретации.`;
    }
}

function generateSummary(results) {
    const wellbeing = parseFloat(results.wellbeing);
    const activity = parseFloat(results.activity);
    const mood = parseFloat(results.mood);
    const total = parseFloat(results.total);
    
    let summary = `Ваше общее состояние оценивается в ${total} баллов из 7. `;
    
    // Анализ общего состояния
    if (total >= 5.5) {
        summary += "У вас отличное общее состояние с высокими показателями по всем параметрам. ";
    } else if (total >= 4.5) {
        summary += "Ваше состояние в целом благоприятное с некоторыми колебаниями. ";
    } else if (total >= 3.5) {
        summary += "Состояние характеризуется нестабильностью и требует внимания. ";
    } else {
        summary += "Общее состояние требует серьезного внимания и возможной поддержки. ";
    }
    
    // Анализ соотношения показателей
    const maxScore = Math.max(wellbeing, activity, mood);
    const minScore = Math.min(wellbeing, activity, mood);
    const difference = maxScore - minScore;
    
    if (difference <= 1.0) {
        summary += "Все показатели находятся в гармонии друг с другом. ";
    } else if (difference <= 2.0) {
        summary += "Наблюдается некоторый дисбаланс между показателями. ";
    } else {
        summary += "Выраженный дисбаланс между различными аспектами состояния. ";
    }
    
    // Определение ведущего показателя
    if (wellbeing === maxScore) {
        summary += "Ведущим фактором является самочувствие. ";
    } else if (activity === maxScore) {
        summary += "Ведущим фактором является активность. ";
    } else {
        summary += "Ведущим фактором является настроение. ";
    }
    
    return summary;
}

function generateRecommendations(results) {
    const wellbeing = parseFloat(results.wellbeing);
    const activity = parseFloat(results.activity);
    const mood = parseFloat(results.mood);
    const total = parseFloat(results.total);
    
    let recommendations = "<ul>";
    
    // Общие рекомендации по общему баллу
    if (total < 4.0) {
        recommendations += "<li><strong>Консультация специалиста:</strong> Рекомендуется обратиться к психологу или врачу для оценки состояния</li>";
        recommendations += "<li><strong>Поддержка окружения:</strong> Обратитесь за поддержкой к близким людям</li>";
    }
    
    // Рекомендации по самочувствию
    if (wellbeing < 4.0) {
        recommendations += "<li><strong>Физическое здоровье:</strong> Обратите внимание на режим сна, питания и физическую активность</li>";
        recommendations += "<li><strong>Отдых:</strong> Уделите время полноценному отдыху и восстановлению</li>";
    } else if (wellbeing > 6.0) {
        recommendations += "<li><strong>Поддержание формы:</strong> Продолжайте заботиться о своем здоровье</li>";
    }
    
    // Рекомендации по активности
    if (activity < 4.0) {
        recommendations += "<li><strong>Постепенная активизация:</strong> Начните с небольших, но регулярных активностей</li>";
        recommendations += "<li><strong>Интересы:</strong> Найдите новые увлечения или вернитесь к старым</li>";
    } else if (activity > 6.0) {
        recommendations += "<li><strong>Баланс:</strong> Убедитесь, что высокая активность не приводит к переутомлению</li>";
    }
    
    // Рекомендации по настроению
    if (mood < 4.0) {
        recommendations += "<li><strong>Позитивные активности:</strong> Занимайтесь тем, что приносит радость</li>";
        recommendations += "<li><strong>Социальные контакты:</strong> Проводите время с близкими и друзьями</li>";
        recommendations += "<li><strong>Профессиональная помощь:</strong> Рассмотрите возможность консультации с психологом</li>";
    } else if (mood > 6.0) {
        recommendations += "<li><strong>Поддержание позитива:</strong> Продолжайте практиковать то, что улучшает ваше настроение</li>";
    }
    
    // Общие рекомендации для всех
    recommendations += "<li><strong>Регулярная оценка:</strong> Повторяйте тест периодически для отслеживания динамики</li>";
    recommendations += "<li><strong>Здоровый образ жизни:</strong> Поддерживайте баланс работы, отдыха и развлечений</li>";
    
    recommendations += "</ul>";
    
    return recommendations;
}

function restartTest() {
    if (currentTest === 'san') {
        startSanTest();
    }
}

// Функции для теста PSM-25
function startPsm25Test() {
    currentQuestion = 0;
    testAnswers = {};
    
    showScreen('psm25-test-screen');
    displayPsm25Question();
    updatePsm25Progress();
    updatePsm25NavigationButtons();
}

function displayPsm25Question() {
    const question = psm25TestData.questions[currentQuestion];
    const container = document.getElementById('psm25-question-container');
    
    container.innerHTML = `
        <div class="question-item">
            <div class="question-number">Вопрос ${question.id}</div>
            <div class="question-pair">
                <div class="question-text">${question.text}</div>
                <div class="psm25-scale">
                    <div class="psm25-scale-option">
                        <input type="radio" name="question_${question.id}" value="1" id="q${question.id}_1">
                        <span class="psm25-scale-number">1</span>
                    </div>
                    <div class="psm25-scale-option">
                        <input type="radio" name="question_${question.id}" value="2" id="q${question.id}_2">
                        <span class="psm25-scale-number">2</span>
                    </div>
                    <div class="psm25-scale-option">
                        <input type="radio" name="question_${question.id}" value="3" id="q${question.id}_3">
                        <span class="psm25-scale-number">3</span>
                    </div>
                    <div class="psm25-scale-option">
                        <input type="radio" name="question_${question.id}" value="4" id="q${question.id}_4">
                        <span class="psm25-scale-number">4</span>
                    </div>
                    <div class="psm25-scale-option">
                        <input type="radio" name="question_${question.id}" value="5" id="q${question.id}_5">
                        <span class="psm25-scale-number">5</span>
                    </div>
                    <div class="psm25-scale-option">
                        <input type="radio" name="question_${question.id}" value="6" id="q${question.id}_6">
                        <span class="psm25-scale-number">6</span>
                    </div>
                    <div class="psm25-scale-option">
                        <input type="radio" name="question_${question.id}" value="7" id="q${question.id}_7">
                        <span class="psm25-scale-number">7</span>
                    </div>
                    <div class="psm25-scale-option">
                        <input type="radio" name="question_${question.id}" value="8" id="q${question.id}_8">
                        <span class="psm25-scale-number">8</span>
                    </div>
                </div>
            </div>
        </div>
    `;
    
    // Восстановить ответ если есть
    if (testAnswers[question.id] !== undefined) {
        const radio = document.querySelector(`input[name="question_${question.id}"][value="${testAnswers[question.id]}"]`);
        if (radio) radio.checked = true;
    }
    
    // Обработчики для радиокнопок
    document.querySelectorAll(`input[name="question_${question.id}"]`).forEach(radio => {
        radio.addEventListener('change', function() {
            testAnswers[question.id] = parseInt(this.value);
            updatePsm25NavigationButtons();
        });
    });
    
    // Обработчики клика для опций шкалы PSM-25
    document.querySelectorAll(`.psm25-scale-option`).forEach(option => {
        option.addEventListener('click', function() {
            const radio = this.querySelector('input[type="radio"]');
            if (radio) {
                radio.checked = true;
                testAnswers[question.id] = parseInt(radio.value);
                updatePsm25NavigationButtons();
            }
        });
    });
    
    document.getElementById('psm25-current-question').textContent = currentQuestion + 1;
}

function updatePsm25Progress() {
    const progress = ((currentQuestion + 1) / psm25TestData.questions.length) * 100;
    document.getElementById('psm25-progress').style.width = progress + '%';
}

function updatePsm25NavigationButtons() {
    const nextBtn = document.getElementById('psm25-next-question');
    const prevBtn = document.getElementById('psm25-prev-question');
    const finishBtn = document.getElementById('psm25-finish-test');
    
    prevBtn.disabled = currentQuestion === 0;
    
    if (currentQuestion === psm25TestData.questions.length - 1) {
        nextBtn.style.display = 'none';
        finishBtn.style.display = 'inline-block';
        finishBtn.disabled = testAnswers[psm25TestData.questions[currentQuestion].id] === undefined;
    } else {
        nextBtn.style.display = 'inline-block';
        finishBtn.style.display = 'none';
        nextBtn.disabled = testAnswers[psm25TestData.questions[currentQuestion].id] === undefined;
    }
}

function nextPsm25Question() {
    if (currentQuestion < psm25TestData.questions.length - 1) {
        currentQuestion++;
        displayPsm25Question();
        updatePsm25Progress();
        updatePsm25NavigationButtons();
    }
}

function prevPsm25Question() {
    if (currentQuestion > 0) {
        currentQuestion--;
        displayPsm25Question();
        updatePsm25Progress();
        updatePsm25NavigationButtons();
    }
}

function finishPsm25Test() {
    calculatePsm25Results();
}

function calculatePsm25Results() {
    const results = {
        total: 0,
        level: 0
    };
    
    // Подсчет общего балла
    Object.values(testAnswers).forEach(answer => {
        results.total += answer;
    });
    
    // Расчет среднего уровня стресса
    results.level = (results.total / psm25TestData.questions.length).toFixed(1);
    
    // Сохранение результатов
    const completedTests = JSON.parse(localStorage.getItem('completedTests') || '{}');
    completedTests.psm25 = {
        results: results,
        timestamp: new Date().toISOString(),
        userInfo: userInfo
    };
    localStorage.setItem('completedTests', JSON.stringify(completedTests));
    
    // Отправка результатов в Telegram
    sendTestResultsToTelegram(results, userInfo, 'PSM-25');
    
    // Отображение результатов
    displayPsm25Results(results);
}

function displayPsm25Results(results) {
    // Отображение баллов
    document.getElementById('psm25-total-score').textContent = results.total;
    document.getElementById('psm25-level-score').textContent = results.level;
    
    // Интерпретация результатов
    document.getElementById('psm25-total-interpretation').textContent = interpretPsm25TotalScore(results.total);
    document.getElementById('psm25-level-interpretation').textContent = interpretPsm25Level(results.level);
    
    // Дополнительная информация
    document.getElementById('psm25-summary-text').textContent = generatePsm25Summary(results);
    document.getElementById('psm25-recommendations-text').innerHTML = generatePsm25Recommendations(results);
    
    showScreen('psm25-results-screen');
}

function interpretPsm25TotalScore(score) {
    if (score <= 50) {
        return `Низкий уровень стресса (${score} баллов). У вас хорошая стрессоустойчивость.`;
    } else if (score <= 100) {
        return `Умеренный уровень стресса (${score} баллов). Рекомендуется обратить внимание на стресс-факторы.`;
    } else if (score <= 150) {
        return `Высокий уровень стресса (${score} баллов). Необходимо принять меры по снижению стресса.`;
    } else {
        return `Очень высокий уровень стресса (${score} баллов). Рекомендуется консультация специалиста.`;
    }
}

function interpretPsm25Level(level) {
    const numLevel = parseFloat(level);
    if (numLevel <= 2) {
        return `Очень низкий (${level}/8). Отличная стрессоустойчивость.`;
    } else if (numLevel <= 4) {
        return `Низкий (${level}/8). Хорошая адаптация к стрессу.`;
    } else if (numLevel <= 6) {
        return `Умеренный (${level}/8). Требуется внимание к стресс-факторам.`;
    } else {
        return `Высокий (${level}/8). Необходима помощь в управлении стрессом.`;
    }
}

function generatePsm25Summary(results) {
    let summary = `Ваш общий балл стресса составляет ${results.total} из 200 возможных. `;
    summary += `Средний уровень стресса: ${results.level} из 8. `;
    
    if (results.total <= 50) {
        summary += "У вас отличная стрессоустойчивость и хорошие адаптивные способности.";
    } else if (results.total <= 100) {
        summary += "У вас умеренный уровень стресса, который можно контролировать с помощью профилактических мер.";
    } else if (results.total <= 150) {
        summary += "У вас высокий уровень стресса, требующий активного вмешательства и изменения образа жизни.";
    } else {
        summary += "У вас критически высокий уровень стресса, требующий профессиональной помощи.";
    }
    
    return summary;
}

function generatePsm25Recommendations(results) {
    let recommendations = "<ul>";
    
    if (results.total <= 50) {
        recommendations += "<li><strong>Поддержание уровня:</strong> Продолжайте практиковать здоровые привычки</li>";
        recommendations += "<li><strong>Профилактика:</strong> Регулярно занимайтесь спортом и медитацией</li>";
    } else if (results.total <= 100) {
        recommendations += "<li><strong>Релаксация:</strong> Изучите техники дыхания и медитации</li>";
        recommendations += "<li><strong>Время для себя:</strong> Выделяйте время на хобби и отдых</li>";
        recommendations += "<li><strong>Физическая активность:</strong> Регулярные упражнения помогут снизить стресс</li>";
    } else if (results.total <= 150) {
        recommendations += "<li><strong>Профессиональная помощь:</strong> Рассмотрите консультацию психолога</li>";
        recommendations += "<li><strong>Изменение образа жизни:</strong> Пересмотрите приоритеты и нагрузки</li>";
        recommendations += "<li><strong>Техники управления стрессом:</strong> Изучите методы когнитивно-поведенческой терапии</li>";
    } else {
        recommendations += "<li><strong>Немедленная помощь:</strong> Обратитесь к специалисту по психическому здоровью</li>";
        recommendations += "<li><strong>Поддержка окружения:</strong> Обратитесь за поддержкой к близким</li>";
        recommendations += "<li><strong>Кризисная помощь:</strong> При необходимости используйте горячие линии психологической помощи</li>";
    }
    
    recommendations += "<li><strong>Регулярная оценка:</strong> Повторяйте тест для отслеживания динамики</li>";
    recommendations += "<li><strong>Здоровый сон:</strong> Обеспечьте 7-9 часов качественного сна</li>";
    recommendations += "<li><strong>Сбалансированное питание:</strong> Поддерживайте здоровый рацион</li>";
    
    recommendations += "</ul>";
    
    return recommendations;
}

function restartPsm25Test() {
    startPsm25Test();
}

// Функции для теста выгорания
function startBurnoutTest() {
    burnoutCurrentQuestion = 0;
    burnoutAnswers = {};
    showScreen('burnout-test-screen');
    displayBurnoutQuestion();
    updateBurnoutProgress();
    updateBurnoutNavigationButtons();
}

function displayBurnoutQuestion() {
    const question = burnoutTestData.questions[burnoutCurrentQuestion];
    const container = document.getElementById('burnout-question-container');
    
    container.innerHTML = `
        <div class="question">
            <h3>${question.id}. ${question.text}</h3>
            <div class="burnout-radio-scale">
                <label class="burnout-radio-option">
                    <input type="radio" name="burnout-answer" value="0" data-value="0">
                    <span class="burnout-radio-number">0</span>
                </label>
                <label class="burnout-radio-option">
                    <input type="radio" name="burnout-answer" value="1" data-value="1">
                    <span class="burnout-radio-number">1</span>
                </label>
                <label class="burnout-radio-option">
                    <input type="radio" name="burnout-answer" value="2" data-value="2">
                    <span class="burnout-radio-number">2</span>
                </label>
                <label class="burnout-radio-option">
                    <input type="radio" name="burnout-answer" value="3" data-value="3">
                    <span class="burnout-radio-number">3</span>
                </label>
                <label class="burnout-radio-option">
                    <input type="radio" name="burnout-answer" value="4" data-value="4">
                    <span class="burnout-radio-number">4</span>
                </label>
                <label class="burnout-radio-option">
                    <input type="radio" name="burnout-answer" value="5" data-value="5">
                    <span class="burnout-radio-number">5</span>
                </label>
                <label class="burnout-radio-option">
                    <input type="radio" name="burnout-answer" value="6" data-value="6">
                    <span class="burnout-radio-number">6</span>
                </label>
            </div>
        </div>
    `;
    
    // Добавить обработчики событий для радиокнопок
    container.querySelectorAll('input[name="burnout-answer"]').forEach(radio => {
        radio.addEventListener('change', function() {
            if (this.checked) {
                // Сохранить ответ
                const value = parseInt(this.value);
                burnoutAnswers[question.id] = value;
                
                // Обновить кнопки навигации
                updateBurnoutNavigationButtons();
            }
        });
    });
    
    // Восстановить выбранный ответ, если он есть
    if (burnoutAnswers[question.id] !== undefined) {
        const selectedRadio = container.querySelector(`input[value="${burnoutAnswers[question.id]}"]`);
        if (selectedRadio) {
            selectedRadio.checked = true;
        }
    }
}

function updateBurnoutProgress() {
    const progress = ((burnoutCurrentQuestion + 1) / burnoutTestData.questions.length) * 100;
    document.getElementById('burnout-progress').style.width = progress + '%';
    document.getElementById('burnout-current-question').textContent = burnoutCurrentQuestion + 1;
}

function updateBurnoutNavigationButtons() {
    const prevBtn = document.getElementById('burnout-prev-question');
    const nextBtn = document.getElementById('burnout-next-question');
    const finishBtn = document.getElementById('burnout-finish-test');
    
    prevBtn.disabled = burnoutCurrentQuestion === 0;
    
    if (burnoutCurrentQuestion === burnoutTestData.questions.length - 1) {
        nextBtn.style.display = 'none';
        finishBtn.style.display = 'inline-block';
        finishBtn.disabled = burnoutAnswers[burnoutTestData.questions[burnoutCurrentQuestion].id] === undefined;
    } else {
        nextBtn.style.display = 'inline-block';
        finishBtn.style.display = 'none';
        nextBtn.disabled = burnoutAnswers[burnoutTestData.questions[burnoutCurrentQuestion].id] === undefined;
    }
}

function nextBurnoutQuestion() {
    if (burnoutCurrentQuestion < burnoutTestData.questions.length - 1) {
        burnoutCurrentQuestion++;
        displayBurnoutQuestion();
        updateBurnoutProgress();
        updateBurnoutNavigationButtons();
    }
}

function prevBurnoutQuestion() {
    if (burnoutCurrentQuestion > 0) {
        burnoutCurrentQuestion--;
        displayBurnoutQuestion();
        updateBurnoutProgress();
        updateBurnoutNavigationButtons();
    }
}

function finishBurnoutTest() {
    calculateBurnoutResults();
    displayBurnoutResults();
    
    // Сохранить статус прохождения теста
    const completedTests = JSON.parse(localStorage.getItem('completedTests') || '{}');
    completedTests.burnout = true;
    localStorage.setItem('completedTests', JSON.stringify(completedTests));
    
    showScreen('burnout-results-screen');
}

function calculateBurnoutResults() {
    let emotionalScore = 0;
    let depersonalizationScore = 0;
    let professionalScore = 0;
    
    burnoutTestData.questions.forEach(question => {
        const answer = burnoutAnswers[question.id];
        if (answer !== undefined) {
            let score = answer;
            
            // Обработка обратных вопросов
            if (question.reverse) {
                score = 6 - answer;
            }
            
            if (question.scale === 'emotional') {
                emotionalScore += score;
            } else if (question.scale === 'depersonalization') {
                depersonalizationScore += score;
            } else if (question.scale === 'professional') {
                professionalScore += score;
            }
        }
    });
    
    return {
        emotional: emotionalScore,
        depersonalization: depersonalizationScore,
        professional: professionalScore
    };
}

function displayBurnoutResults() {
    const results = calculateBurnoutResults();
    
    // Отобразить баллы
    document.getElementById('burnout-emotional-score').textContent = results.emotional;
    document.getElementById('burnout-depersonalization-score').textContent = results.depersonalization;
    document.getElementById('burnout-professional-score').textContent = results.professional;
    
    // Определить уровни
    const emotionalLevel = getBurnoutLevel('emotional', results.emotional);
    const depersonalizationLevel = getBurnoutLevel('depersonalization', results.depersonalization);
    const professionalLevel = getBurnoutLevel('professional', results.professional);
    
    // Отобразить уровни
    const emotionalLevelEl = document.getElementById('burnout-emotional-level');
    const depersonalizationLevelEl = document.getElementById('burnout-depersonalization-level');
    const professionalLevelEl = document.getElementById('burnout-professional-level');
    
    emotionalLevelEl.textContent = emotionalLevel.text;
    emotionalLevelEl.className = `level-indicator ${emotionalLevel.class}`;
    
    depersonalizationLevelEl.textContent = depersonalizationLevel.text;
    depersonalizationLevelEl.className = `level-indicator ${depersonalizationLevel.class}`;
    
    professionalLevelEl.textContent = professionalLevel.text;
    professionalLevelEl.className = `level-indicator ${professionalLevel.class}`;
    
    // Генерировать сводку и рекомендации
    generateBurnoutSummary(results, emotionalLevel, depersonalizationLevel, professionalLevel);
    
    // Отправить результаты в Telegram
    sendTestResultsToTelegram({
        emotional: results.emotional,
        depersonalization: results.depersonalization,
        professional: results.professional,
        emotionalLevel: emotionalLevel.text,
        depersonalizationLevel: depersonalizationLevel.text,
        professionalLevel: professionalLevel.text
    }, userInfo, 'Опросник выгорания');
}

function getBurnoutLevel(scale, score) {
    if (scale === 'emotional') {
        if (score <= 15) return { text: 'Низкий уровень', class: 'low' };
        if (score <= 24) return { text: 'Средний уровень', class: 'medium' };
        return { text: 'Высокий уровень', class: 'high' };
    } else if (scale === 'depersonalization') {
        if (score <= 5) return { text: 'Низкий уровень', class: 'low' };
        if (score <= 10) return { text: 'Средний уровень', class: 'medium' };
        return { text: 'Высокий уровень', class: 'high' };
    } else if (scale === 'professional') {
        if (score >= 37) return { text: 'Низкий уровень', class: 'low' };
        if (score >= 31) return { text: 'Средний уровень', class: 'medium' };
        return { text: 'Высокий уровень', class: 'high' };
    }
}

function generateBurnoutSummary(results, emotionalLevel, depersonalizationLevel, professionalLevel) {
    const summary = document.getElementById('burnout-summary');
    const recommendations = document.getElementById('burnout-recommendations');
    
    let summaryText = '<h3>Интерпретация результатов</h3>';
    summaryText += '<p><strong>Эмоциональное истощение</strong> - чувство эмоциональной опустошенности и усталости от работы.</p>';
    summaryText += '<p><strong>Деперсонализация</strong> - циничное отношение к работе и людям, с которыми приходится работать.</p>';
    summaryText += '<p><strong>Редукция профессионализма</strong> - ощущение некомпетентности и неуспешности в профессиональной деятельности.</p>';
    
    let recommendationsText = '<h3>Рекомендации</h3>';
    
    if (emotionalLevel.class === 'high' || depersonalizationLevel.class === 'high' || professionalLevel.class === 'high') {
        recommendationsText += '<p><strong>Высокий уровень выгорания:</strong></p>';
        recommendationsText += '<ul>';
        recommendationsText += '<li>Обратитесь за профессиональной помощью к психологу</li>';
        recommendationsText += '<li>Рассмотрите возможность временного перерыва в работе</li>';
        recommendationsText += '<li>Обсудите ситуацию с руководством</li>';
        recommendationsText += '<li>Практикуйте техники релаксации и медитации</li>';
        recommendationsText += '</ul>';
    } else if (emotionalLevel.class === 'medium' || depersonalizationLevel.class === 'medium' || professionalLevel.class === 'medium') {
        recommendationsText += '<p><strong>Средний уровень выгорания:</strong></p>';
        recommendationsText += '<ul>';
        recommendationsText += '<li>Уделите больше времени отдыху и восстановлению</li>';
        recommendationsText += '<li>Найдите баланс между работой и личной жизнью</li>';
        recommendationsText += '<li>Развивайте навыки управления стрессом</li>';
        recommendationsText += '<li>Общайтесь с коллегами и друзьями</li>';
        recommendationsText += '</ul>';
    } else {
        recommendationsText += '<p><strong>Низкий уровень выгорания:</strong></p>';
        recommendationsText += '<p>Отлично! Продолжайте поддерживать здоровый баланс между работой и личной жизнью.</p>';
    }
    
    summary.innerHTML = summaryText;
    recommendations.innerHTML = recommendationsText;
}

function restartBurnoutTest() {
    startBurnoutTest();
}

// Функции для теста тревоги
function startAnxietyTest() {
    anxietyCurrentQuestion = 0;
    anxietyAnswers = {};
    anxietyCurrentScale = 'state';
    showScreen('anxiety-test-screen');
    updateAnxietyInstruction();
    displayAnxietyQuestion();
    updateAnxietyProgress();
    updateAnxietyNavigationButtons();
}

function updateAnxietyInstruction() {
    const instructionText = document.getElementById('anxiety-instruction-text');
    const scaleText1 = document.getElementById('anxiety-scale-text-1');
    const scaleText2 = document.getElementById('anxiety-scale-text-2');
    const scaleText3 = document.getElementById('anxiety-scale-text-3');
    const scaleText4 = document.getElementById('anxiety-scale-text-4');
    
    if (anxietyCurrentScale === 'state') {
        instructionText.textContent = 'Прочитайте каждое из приведенных ниже предложений и обведите цифру в соответствующей графе справа в зависимости от самочувствия в данный момент.';
        scaleText1.textContent = 'Нет, это не так';
        scaleText2.textContent = 'Пожалуй, так';
        scaleText3.textContent = 'Верно';
        scaleText4.textContent = 'Совершенно верно';
    } else {
        instructionText.textContent = 'Прочитайте каждое из приведенных ниже предложений и обведите цифру в соответствующей графе справа в зависимости от того, как вы себя чувствуете обычно.';
        scaleText1.textContent = 'Никогда';
        scaleText2.textContent = 'Почти никогда';
        scaleText3.textContent = 'Часто';
        scaleText4.textContent = 'Почти всегда';
    }
}

function displayAnxietyQuestion() {
    const questions = anxietyCurrentScale === 'state' ? anxietyTestData.stateQuestions : anxietyTestData.traitQuestions;
    const question = questions[anxietyCurrentQuestion];
    const container = document.getElementById('anxiety-question-container');
    
    container.innerHTML = `
        <div class="question">
            <h3>${question.id}. ${question.text}</h3>
            <div class="anxiety-radio-scale">
                <label class="anxiety-radio-option">
                    <input type="radio" name="anxiety-answer" value="1" data-value="1">
                    <span class="anxiety-radio-number">1</span>
                </label>
                <label class="anxiety-radio-option">
                    <input type="radio" name="anxiety-answer" value="2" data-value="2">
                    <span class="anxiety-radio-number">2</span>
                </label>
                <label class="anxiety-radio-option">
                    <input type="radio" name="anxiety-answer" value="3" data-value="3">
                    <span class="anxiety-radio-number">3</span>
                </label>
                <label class="anxiety-radio-option">
                    <input type="radio" name="anxiety-answer" value="4" data-value="4">
                    <span class="anxiety-radio-number">4</span>
                </label>
            </div>
        </div>
    `;
    
    // Добавить обработчики событий для радиокнопок
    container.querySelectorAll('input[name="anxiety-answer"]').forEach(radio => {
        radio.addEventListener('change', function() {
            if (this.checked) {
                // Сохранить ответ
                const value = parseInt(this.value);
                anxietyAnswers[question.id] = value;
                
                // Обновить кнопки навигации
                updateAnxietyNavigationButtons();
            }
        });
    });
    
    // Восстановить выбранный ответ, если он есть
    if (anxietyAnswers[question.id] !== undefined) {
        const selectedRadio = container.querySelector(`input[value="${anxietyAnswers[question.id]}"]`);
        if (selectedRadio) {
            selectedRadio.checked = true;
        }
    }
}

function updateAnxietyProgress() {
    const totalQuestions = anxietyTestData.stateQuestions.length + anxietyTestData.traitQuestions.length;
    const currentProgress = anxietyCurrentScale === 'state' ? 
        anxietyCurrentQuestion + 1 : 
        anxietyTestData.stateQuestions.length + anxietyCurrentQuestion + 1;
    
    const progress = (currentProgress / totalQuestions) * 100;
    document.getElementById('anxiety-progress').style.width = progress + '%';
    document.getElementById('anxiety-current-question').textContent = currentProgress;
}

function updateAnxietyNavigationButtons() {
    const prevBtn = document.getElementById('anxiety-prev-question');
    const nextBtn = document.getElementById('anxiety-next-question');
    const finishBtn = document.getElementById('anxiety-finish-test');
    
    const questions = anxietyCurrentScale === 'state' ? anxietyTestData.stateQuestions : anxietyTestData.traitQuestions;
    const isLastQuestionOfScale = anxietyCurrentQuestion === questions.length - 1;
    const isLastQuestionOfTest = anxietyCurrentScale === 'trait' && isLastQuestionOfScale;
    
    prevBtn.disabled = anxietyCurrentQuestion === 0 && anxietyCurrentScale === 'state';
    
    if (isLastQuestionOfTest) {
        nextBtn.style.display = 'none';
        finishBtn.style.display = 'inline-block';
        finishBtn.disabled = anxietyAnswers[questions[anxietyCurrentQuestion].id] === undefined;
    } else {
        nextBtn.style.display = 'inline-block';
        finishBtn.style.display = 'none';
        nextBtn.disabled = anxietyAnswers[questions[anxietyCurrentQuestion].id] === undefined;
    }
}

function nextAnxietyQuestion() {
    const questions = anxietyCurrentScale === 'state' ? anxietyTestData.stateQuestions : anxietyTestData.traitQuestions;
    const isLastQuestionOfScale = anxietyCurrentQuestion === questions.length - 1;
    
    if (isLastQuestionOfScale && anxietyCurrentScale === 'state') {
        // Переход к личностной тревожности
        anxietyCurrentScale = 'trait';
        anxietyCurrentQuestion = 0;
        updateAnxietyInstruction();
    } else if (anxietyCurrentQuestion < questions.length - 1) {
        anxietyCurrentQuestion++;
    }
    
    displayAnxietyQuestion();
    updateAnxietyProgress();
    updateAnxietyNavigationButtons();
}

function prevAnxietyQuestion() {
    if (anxietyCurrentQuestion > 0) {
        anxietyCurrentQuestion--;
    } else if (anxietyCurrentScale === 'trait') {
        // Переход обратно к ситуационной тревожности
        anxietyCurrentScale = 'state';
        anxietyCurrentQuestion = anxietyTestData.stateQuestions.length - 1;
        updateAnxietyInstruction();
    }
    
    displayAnxietyQuestion();
    updateAnxietyProgress();
    updateAnxietyNavigationButtons();
}

function finishAnxietyTest() {
    calculateAnxietyResults();
    displayAnxietyResults();
    
    // Сохранить статус прохождения теста
    const completedTests = JSON.parse(localStorage.getItem('completedTests') || '{}');
    completedTests.anxiety = true;
    localStorage.setItem('completedTests', JSON.stringify(completedTests));
    
    showScreen('anxiety-results-screen');
}

function calculateAnxietyResults() {
    let stateScore = 0;
    let traitScore = 0;
    
    // Расчет ситуационной тревожности
    anxietyTestData.stateQuestions.forEach(question => {
        const answer = anxietyAnswers[question.id];
        if (answer !== undefined) {
            let score = answer;
            if (question.reverse) {
                score = 5 - answer;
            }
            stateScore += score;
        }
    });
    
    // Расчет личностной тревожности
    anxietyTestData.traitQuestions.forEach(question => {
        const answer = anxietyAnswers[question.id];
        if (answer !== undefined) {
            let score = answer;
            if (question.reverse) {
                score = 5 - answer;
            }
            traitScore += score;
        }
    });
    
    // Вычисляем средние значения (сумма баллов / 20)
    const stateAverage = stateScore / 20;
    const traitAverage = traitScore / 20;
    
    return {
        state: stateScore,
        trait: traitScore,
        stateAverage: stateAverage,
        traitAverage: traitAverage
    };
}

function displayAnxietyResults() {
    const results = calculateAnxietyResults();
    
    // Отобразить баллы и средние значения
    document.getElementById('anxiety-state-score').textContent = `${results.state} (средний: ${results.stateAverage.toFixed(1)})`;
    document.getElementById('anxiety-trait-score').textContent = `${results.trait} (средний: ${results.traitAverage.toFixed(1)})`;
    
    // Определить уровни на основе средних значений
    const stateLevel = getAnxietyLevel('state', results.stateAverage);
    const traitLevel = getAnxietyLevel('trait', results.traitAverage);
    
    // Отобразить уровни
    const stateLevelEl = document.getElementById('anxiety-state-level');
    const traitLevelEl = document.getElementById('anxiety-trait-level');
    
    stateLevelEl.textContent = stateLevel.text;
    stateLevelEl.className = `level-indicator ${stateLevel.class}`;
    
    traitLevelEl.textContent = traitLevel.text;
    traitLevelEl.className = `level-indicator ${traitLevel.class}`;
    
    // Генерировать сводку и рекомендации
    generateAnxietySummary(results, stateLevel, traitLevel);
    
    // Отправить результаты в Telegram
    sendTestResultsToTelegram({
        state: results.state,
        trait: results.trait,
        stateAverage: results.stateAverage,
        traitAverage: results.traitAverage,
        stateLevel: stateLevel.text,
        traitLevel: traitLevel.text
    }, userInfo, 'Шкала тревоги');
}

function getAnxietyLevel(scale, score) {
    // Диапазоны для средних значений (1.0-4.0)
    if (score < 1.5) return { text: 'Низкая тревожность', class: 'low' };
    if (score < 2.0) return { text: 'Низкая тревожность', class: 'low' };
    if (score < 3.0) return { text: 'Средняя тревожность', class: 'medium' };
    if (score < 3.5) return { text: 'Высокая тревожность', class: 'high' };
    return { text: 'Очень высокая тревожность', class: 'high' };
}

function generateAnxietySummary(results, stateLevel, traitLevel) {
    const summary = document.getElementById('anxiety-summary');
    const recommendations = document.getElementById('anxiety-recommendations');
    
    let summaryText = '<h3>Интерпретация результатов</h3>';
    summaryText += '<p><strong>Ситуационная тревожность</strong> - временное эмоциональное состояние, характеризующееся субъективными ощущениями напряжения, беспокойства, озабоченности.</p>';
    summaryText += '<p><strong>Личностная тревожность</strong> - устойчивая индивидуальная характеристика, отражающая предрасположенность к тревоге и склонность воспринимать широкий круг ситуаций как угрожающие.</p>';
    
    summaryText += '<h3>Уровни тревожности:</h3>';
    summaryText += '<ul>';
    summaryText += '<li><strong>Низкий (1.5-1.9 балла)</strong> - низкая тревожность</li>';
    summaryText += '<li><strong>Средний (2.0-2.9 балла)</strong> - средняя тревожность</li>';
    summaryText += '<li><strong>Высокий (3.0-3.4 балла)</strong> - высокая тревожность</li>';
    summaryText += '<li><strong>Очень высокий (3.5-4.0 балла)</strong> - очень высокая тревожность</li>';
    summaryText += '</ul>';
    
    let recommendationsText = '<h3>Рекомендации</h3>';
    
    if (stateLevel.class === 'high' || traitLevel.class === 'high') {
        recommendationsText += '<p><strong>Высокий уровень тревожности:</strong></p>';
        recommendationsText += '<ul>';
        recommendationsText += '<li>Обратитесь за консультацией к психологу</li>';
        recommendationsText += '<li>Практикуйте техники релаксации и дыхательные упражнения</li>';
        recommendationsText += '<li>Изучите методы управления стрессом</li>';
        recommendationsText += '<li>Рассмотрите возможность медитации или йоги</li>';
        recommendationsText += '</ul>';
    } else if (stateLevel.class === 'medium' || traitLevel.class === 'medium') {
        recommendationsText += '<p><strong>Средний уровень тревожности:</strong></p>';
        recommendationsText += '<ul>';
        recommendationsText += '<li>Развивайте навыки саморегуляции</li>';
        recommendationsText += '<li>Практикуйте регулярные физические упражнения</li>';
        recommendationsText += '<li>Поддерживайте здоровый режим сна</li>';
        recommendationsText += '<li>Изучайте техники когнитивной реструктуризации</li>';
        recommendationsText += '</ul>';
    } else {
        recommendationsText += '<p><strong>Низкий уровень тревожности:</strong></p>';
        recommendationsText += '<p>Отлично! Продолжайте поддерживать эмоциональное равновесие и здоровый образ жизни.</p>';
    }
    
    summary.innerHTML = summaryText;
    recommendations.innerHTML = recommendationsText;
}

function restartAnxietyTest() {
    startAnxietyTest();
}

// Функции для теста депрессии
function startDepressionTest() {
    depressionCurrentQuestion = 0;
    depressionAnswers = {};
    showScreen('depression-test-screen');
    displayDepressionQuestion();
    updateDepressionProgress();
    updateDepressionNavigationButtons();
}

function displayDepressionQuestion() {
    const questionGroup = getDepressionQuestionGroup(depressionCurrentQuestion + 1);
    const container = document.getElementById('depression-question-container');
    
    container.innerHTML = `
        <div class="question">
            <h3>Вопрос ${depressionCurrentQuestion + 1}</h3>
            <p>Выберите утверждение, которое лучше всего описывает ваше состояние за последнюю неделю:</p>
            <div class="depression-options">
                ${questionGroup.map((option, index) => `
                    <label class="depression-option">
                        <input type="radio" name="depression-answer" value="${option.score}" data-score="${option.score}">
                        <span class="depression-option-text">${option.text}</span>
                    </label>
                `).join('')}
            </div>
        </div>
    `;
    
    // Добавить обработчики событий для радиокнопок
    container.querySelectorAll('input[name="depression-answer"]').forEach(radio => {
        radio.addEventListener('change', function() {
            if (this.checked) {
                // Сохранить ответ
                const score = parseInt(this.value);
                depressionAnswers[depressionCurrentQuestion + 1] = score;
                
                // Обновить кнопки навигации
                updateDepressionNavigationButtons();
            }
        });
    });
    
    // Восстановить выбранный ответ, если он есть
    if (depressionAnswers[depressionCurrentQuestion + 1] !== undefined) {
        const selectedRadio = container.querySelector(`input[value="${depressionAnswers[depressionCurrentQuestion + 1]}"]`);
        if (selectedRadio) {
            selectedRadio.checked = true;
        }
    }
}

function getDepressionQuestionGroup(questionNumber) {
    return beckDepressionTestData.questions.filter(q => q.id === questionNumber);
}

function updateDepressionProgress() {
    const progress = ((depressionCurrentQuestion + 1) / 21) * 100;
    document.getElementById('depression-progress').style.width = progress + '%';
    document.getElementById('depression-current-question').textContent = depressionCurrentQuestion + 1;
}

function updateDepressionNavigationButtons() {
    const prevBtn = document.getElementById('depression-prev-question');
    const nextBtn = document.getElementById('depression-next-question');
    const finishBtn = document.getElementById('depression-finish-test');
    
    prevBtn.disabled = depressionCurrentQuestion === 0;
    
    if (depressionCurrentQuestion === 20) { // 21 вопрос (0-20)
        nextBtn.style.display = 'none';
        finishBtn.style.display = 'inline-block';
        finishBtn.disabled = depressionAnswers[depressionCurrentQuestion + 1] === undefined;
    } else {
        nextBtn.style.display = 'inline-block';
        finishBtn.style.display = 'none';
        nextBtn.disabled = depressionAnswers[depressionCurrentQuestion + 1] === undefined;
    }
}

function nextDepressionQuestion() {
    if (depressionCurrentQuestion < 20) {
        depressionCurrentQuestion++;
        displayDepressionQuestion();
        updateDepressionProgress();
        updateDepressionNavigationButtons();
    }
}

function prevDepressionQuestion() {
    if (depressionCurrentQuestion > 0) {
        depressionCurrentQuestion--;
        displayDepressionQuestion();
        updateDepressionProgress();
        updateDepressionNavigationButtons();
    }
}

function finishDepressionTest() {
    calculateDepressionResults();
    displayDepressionResults();
    
    // Сохранить статус прохождения теста
    const completedTests = JSON.parse(localStorage.getItem('completedTests') || '{}');
    completedTests.depression = true;
    localStorage.setItem('completedTests', JSON.stringify(completedTests));
    
    showScreen('depression-results-screen');
}

function calculateDepressionResults() {
    let totalScore = 0;
    let cognitiveAffectiveScore = 0;
    let somaticScore = 0;
    
    // Подсчет общего балла
    for (let i = 1; i <= 21; i++) {
        const answer = depressionAnswers[i];
        if (answer !== undefined) {
            totalScore += answer;
        }
    }
    
    // Подсчет субшкал
    // Когнитивно-аффективная субшкала (вопросы 1-13)
    for (let i = 1; i <= 13; i++) {
        const answer = depressionAnswers[i];
        if (answer !== undefined) {
            cognitiveAffectiveScore += answer;
        }
    }
    
    // Соматическая субшкала (вопросы 14-21)
    for (let i = 14; i <= 21; i++) {
        const answer = depressionAnswers[i];
        if (answer !== undefined) {
            somaticScore += answer;
        }
    }
    
    // Определение уровня депрессии
    let level = '';
    if (totalScore <= 9) {
        level = 'Отсутствие депрессивных симптомов';
    } else if (totalScore <= 15) {
        level = 'Легкая депрессия (субдепрессия)';
    } else if (totalScore <= 19) {
        level = 'Умеренная депрессия';
    } else if (totalScore <= 29) {
        level = 'Выраженная депрессия (средней тяжести)';
    } else {
        level = 'Тяжелая депрессия';
    }
    
    return {
        total: totalScore,
        cognitiveAffective: cognitiveAffectiveScore,
        somatic: somaticScore,
        level: level
    };
}

function displayDepressionResults() {
    const results = calculateDepressionResults();
    
    // Отобразить баллы
    document.getElementById('depression-total-score').textContent = results.total;
    document.getElementById('depression-cognitive-affective-score').textContent = results.cognitiveAffective;
    document.getElementById('depression-somatic-score').textContent = results.somatic;
    document.getElementById('depression-level').textContent = results.level;
    
    // Генерировать сводку и рекомендации
    generateDepressionSummary(results);
    
    // Отправить результаты в Telegram
    sendTestResultsToTelegram(results, userInfo, 'Шкала депрессии');
}

function generateDepressionSummary(results) {
    const summary = document.getElementById('depression-summary');
    const recommendations = document.getElementById('depression-recommendations');
    
    let summaryText = '<h3>Интерпретация результатов</h3>';
    summaryText += `<p><strong>Общий балл:</strong> ${results.total} из 63 возможных</p>`;
    summaryText += `<p><strong>Уровень депрессии:</strong> ${results.level}</p>`;
    summaryText += `<p><strong>Когнитивно-аффективная субшкала:</strong> ${results.cognitiveAffective} баллов (вопросы 1-13)</p>`;
    summaryText += `<p><strong>Соматическая субшкала:</strong> ${results.somatic} баллов (вопросы 14-21)</p>`;
    
    summaryText += '<h3>Шкала оценки:</h3>';
    summaryText += '<ul>';
    summaryText += '<li><strong>0-9 баллов:</strong> Отсутствие депрессивных симптомов</li>';
    summaryText += '<li><strong>10-15 баллов:</strong> Легкая депрессия (субдепрессия)</li>';
    summaryText += '<li><strong>16-19 баллов:</strong> Умеренная депрессия</li>';
    summaryText += '<li><strong>20-29 баллов:</strong> Выраженная депрессия (средней тяжести)</li>';
    summaryText += '<li><strong>30-63 балла:</strong> Тяжелая депрессия</li>';
    summaryText += '</ul>';
    
    let recommendationsText = '<h3>Рекомендации</h3>';
    
    if (results.total <= 9) {
        recommendationsText += '<p><strong>Отличные результаты!</strong></p>';
        recommendationsText += '<ul>';
        recommendationsText += '<li>Продолжайте поддерживать здоровый образ жизни</li>';
        recommendationsText += '<li>Регулярно занимайтесь физическими упражнениями</li>';
        recommendationsText += '<li>Поддерживайте социальные связи</li>';
        recommendationsText += '<li>Практикуйте техники управления стрессом</li>';
        recommendationsText += '</ul>';
    } else if (results.total <= 15) {
        recommendationsText += '<p><strong>Легкая депрессия:</strong></p>';
        recommendationsText += '<ul>';
        recommendationsText += '<li>Обратите внимание на режим сна и питания</li>';
        recommendationsText += '<li>Увеличьте физическую активность</li>';
        recommendationsText += '<li>Проводите время с близкими людьми</li>';
        recommendationsText += '<li>Рассмотрите консультацию психолога</li>';
        recommendationsText += '</ul>';
    } else if (results.total <= 19) {
        recommendationsText += '<p><strong>Умеренная депрессия:</strong></p>';
        recommendationsText += '<ul>';
        recommendationsText += '<li>Рекомендуется консультация психолога или психиатра</li>';
        recommendationsText += '<li>Рассмотрите возможность психотерапии</li>';
        recommendationsText += '<li>Обратитесь за поддержкой к близким</li>';
        recommendationsText += '<li>Соблюдайте режим дня</li>';
        recommendationsText += '</ul>';
    } else if (results.total <= 29) {
        recommendationsText += '<p><strong>Выраженная депрессия:</strong></p>';
        recommendationsText += '<ul>';
        recommendationsText += '<li>Немедленно обратитесь к психиатру</li>';
        recommendationsText += '<li>Рассмотрите возможность медикаментозного лечения</li>';
        recommendationsText += '<li>Обратитесь за поддержкой к близким</li>';
        recommendationsText += '<li>При необходимости используйте горячие линии психологической помощи</li>';
        recommendationsText += '</ul>';
    } else {
        recommendationsText += '<p><strong>Тяжелая депрессия:</strong></p>';
        recommendationsText += '<ul>';
        recommendationsText += '<li>Немедленно обратитесь за профессиональной помощью</li>';
        recommendationsText += '<li>Рассмотрите госпитализацию при необходимости</li>';
        recommendationsText += '<li>Обратитесь за поддержкой к близким</li>';
        recommendationsText += '<li>Используйте кризисные службы психологической помощи</li>';
        recommendationsText += '</ul>';
    }
    
    summary.innerHTML = summaryText;
    recommendations.innerHTML = recommendationsText;
}

function restartDepressionTest() {
    startDepressionTest();
}

// Функции для теста на буллинг
function startBullyingTest() {
    bullyingCurrentSection = 0;
    bullyingCurrentQuestion = 0;
    bullyingAnswers = {};
    showScreen('bullying-test-screen');
    displayBullyingQuestion();
    updateBullyingProgress();
    updateBullyingNavigationButtons();
}

function displayBullyingQuestion() {
    const section = bullyingTestData.sections[bullyingCurrentSection];
    const question = section.questions[bullyingCurrentQuestion];
    const container = document.getElementById('bullying-question-container');
    
    container.innerHTML = `
        <div class="question">
            <h3>${section.title}</h3>
            <div class="bullying-question-text">${question.text}</div>
            <div class="bullying-radio-scale">
                <label class="bullying-radio-option">
                    <input type="radio" name="bullying-answer" value="0" data-value="0">
                    <span class="bullying-radio-text">НЕТ</span>
                </label>
                <label class="bullying-radio-option">
                    <input type="radio" name="bullying-answer" value="1" data-value="1">
                    <span class="bullying-radio-text">ДА</span>
                </label>
            </div>
        </div>
    `;
    
    // Добавить обработчики событий для радиокнопок
    container.querySelectorAll('input[name="bullying-answer"]').forEach(radio => {
        radio.addEventListener('change', function() {
            if (this.checked) {
                // Сохранить ответ
                const value = parseInt(this.value);
                bullyingAnswers[question.id] = value;
                
                // Обновить кнопки навигации
                updateBullyingNavigationButtons();
            }
        });
    });
    
    // Восстановить выбранный ответ, если он есть
    if (bullyingAnswers[question.id] !== undefined) {
        const selectedRadio = container.querySelector(`input[value="${bullyingAnswers[question.id]}"]`);
        if (selectedRadio) {
            selectedRadio.checked = true;
        }
    }
}

function updateBullyingProgress() {
    const totalQuestions = 48; // Общее количество вопросов в тесте
    let currentQuestionNumber = 0;
    
    for (let i = 0; i < bullyingCurrentSection; i++) {
        currentQuestionNumber += bullyingTestData.sections[i].questions.length;
    }
    currentQuestionNumber += bullyingCurrentQuestion + 1;
    
    const progress = (currentQuestionNumber / totalQuestions) * 100;
    document.getElementById('bullying-progress').style.width = progress + '%';
    document.getElementById('bullying-current-question').textContent = currentQuestionNumber;
}

function updateBullyingNavigationButtons() {
    const prevBtn = document.getElementById('bullying-prev-question');
    const nextBtn = document.getElementById('bullying-next-question');
    const finishBtn = document.getElementById('bullying-finish-test');
    
    const section = bullyingTestData.sections[bullyingCurrentSection];
    const isLastQuestionOfSection = bullyingCurrentQuestion === section.questions.length - 1;
    const isLastSection = bullyingCurrentSection === bullyingTestData.sections.length - 1;
    const isLastQuestionOfTest = isLastSection && isLastQuestionOfSection;
    
    prevBtn.disabled = bullyingCurrentSection === 0 && bullyingCurrentQuestion === 0;
    
    if (isLastQuestionOfTest) {
        nextBtn.style.display = 'none';
        finishBtn.style.display = 'inline-block';
        finishBtn.disabled = bullyingAnswers[section.questions[bullyingCurrentQuestion].id] === undefined;
    } else {
        nextBtn.style.display = 'inline-block';
        finishBtn.style.display = 'none';
        nextBtn.disabled = bullyingAnswers[section.questions[bullyingCurrentQuestion].id] === undefined;
    }
}

function nextBullyingQuestion() {
    const section = bullyingTestData.sections[bullyingCurrentSection];
    const isLastQuestionOfSection = bullyingCurrentQuestion === section.questions.length - 1;
    
    if (isLastQuestionOfSection) {
        // Переход к следующей секции
        if (bullyingCurrentSection < bullyingTestData.sections.length - 1) {
            bullyingCurrentSection++;
            bullyingCurrentQuestion = 0;
        }
    } else {
        // Переход к следующему вопросу в секции
        bullyingCurrentQuestion++;
    }
    
    displayBullyingQuestion();
    updateBullyingProgress();
    updateBullyingNavigationButtons();
}

function prevBullyingQuestion() {
    if (bullyingCurrentQuestion > 0) {
        bullyingCurrentQuestion--;
    } else if (bullyingCurrentSection > 0) {
        // Переход к предыдущей секции
        bullyingCurrentSection--;
        bullyingCurrentQuestion = bullyingTestData.sections[bullyingCurrentSection].questions.length - 1;
    }
    
    displayBullyingQuestion();
    updateBullyingProgress();
    updateBullyingNavigationButtons();
}

function finishBullyingTest() {
    calculateBullyingResults();
    displayBullyingResults();
    
    // Сохранить статус прохождения теста
    const completedTests = JSON.parse(localStorage.getItem('completedTests') || '{}');
    completedTests.bullying = true;
    localStorage.setItem('completedTests', JSON.stringify(completedTests));
    
    showScreen('bullying-results-screen');
}

function calculateBullyingResults() {
    const results = {
        insecurity: 0,
        wellbeing: 0,
        disunity: 0,
        equality: 0
    };
    
    // Подсчет баллов по шкалам
    bullyingTestData.sections.forEach(section => {
        section.questions.forEach(question => {
            const answer = bullyingAnswers[question.id];
            if (answer !== undefined) {
                let score = answer;
                
                // Обработка обратных вопросов
                if (question.reverse) {
                    score = 1 - answer;
                }
                
                results[question.scale] += score;
            }
        });
    });
    
    // Определение уровней для каждой шкалы
    results.insecurityLevel = getBullyingLevel('insecurity', results.insecurity);
    results.wellbeingLevel = getBullyingLevel('wellbeing', results.wellbeing);
    results.disunityLevel = getBullyingLevel('disunity', results.disunity);
    results.equalityLevel = getBullyingLevel('equality', results.equality);
    
    return results;
}

function getBullyingLevel(scale, score) {
    // Нормативные значения на основе таблицы из изображения
    const norms = {
        insecurity: { low: 0, medium: 8, high: 12 },
        wellbeing: { low: 0, medium: 6, high: 9 },
        disunity: { low: 0, medium: 2, high: 4 },
        equality: { low: 0, medium: 3, high: 6 }
    };
    
    const norm = norms[scale];
    if (score <= norm.low) return 'Низкий';
    if (score <= norm.medium) return 'Средний';
    if (score <= norm.high) return 'Высокий';
    return 'Очень высокий';
}

function displayBullyingResults() {
    const results = calculateBullyingResults();
    
    // Отобразить баллы
    document.getElementById('bullying-insecurity-score').textContent = results.insecurity;
    document.getElementById('bullying-wellbeing-score').textContent = results.wellbeing;
    document.getElementById('bullying-disunity-score').textContent = results.disunity;
    document.getElementById('bullying-equality-score').textContent = results.equality;
    
    // Отобразить уровни
    const insecurityLevelEl = document.getElementById('bullying-insecurity-level');
    const wellbeingLevelEl = document.getElementById('bullying-wellbeing-level');
    const disunityLevelEl = document.getElementById('bullying-disunity-level');
    const equalityLevelEl = document.getElementById('bullying-equality-level');
    
    insecurityLevelEl.textContent = results.insecurityLevel;
    insecurityLevelEl.className = `level-indicator ${getBullyingLevelClass(results.insecurityLevel)}`;
    
    wellbeingLevelEl.textContent = results.wellbeingLevel;
    wellbeingLevelEl.className = `level-indicator ${getBullyingLevelClass(results.wellbeingLevel)}`;
    
    disunityLevelEl.textContent = results.disunityLevel;
    disunityLevelEl.className = `level-indicator ${getBullyingLevelClass(results.disunityLevel)}`;
    
    equalityLevelEl.textContent = results.equalityLevel;
    equalityLevelEl.className = `level-indicator ${getBullyingLevelClass(results.equalityLevel)}`;
    
    // Генерировать сводку и рекомендации
    generateBullyingSummary(results);
    
    // Отправить результаты в Telegram
    sendTestResultsToTelegram(results, userInfo, 'Опросник риска буллинга');
}

function getBullyingLevelClass(level) {
    switch(level) {
        case 'Низкий': return 'low';
        case 'Средний': return 'medium';
        case 'Высокий': return 'high';
        case 'Очень высокий': return 'high';
        default: return 'medium';
    }
}

function generateBullyingSummary(results) {
    const summary = document.getElementById('bullying-summary');
    const recommendations = document.getElementById('bullying-recommendations');
    
    let summaryText = '<h3>Интерпретация результатов</h3>';
    summaryText += '<p><strong>Шкала небезопасности</strong> - характеризует распространенность неуважения, небезопасности и пренебрежения правилами и границами в классе.</p>';
    summaryText += '<p><strong>Шкала благополучия</strong> - характеризует стабильность границ, соблюдение правил и установление уважения как нормы в группе.</p>';
    summaryText += '<p><strong>Шкала разобщенности</strong> - выявляет отсутствие сплоченности, степень дистанции между подростками и между подростками и учителями.</p>';
    summaryText += '<p><strong>Шкала равноправия</strong> - оценивает способность группы принимать различия между участниками, распределять роли и вступать в конструктивное общение.</p>';
    
    let recommendationsText = '<h3>Рекомендации</h3>';
    
    if (results.insecurityLevel === 'Высокий' || results.insecurityLevel === 'Очень высокий') {
        recommendationsText += '<p><strong>Высокий уровень небезопасности:</strong></p>';
        recommendationsText += '<ul>';
        recommendationsText += '<li>Необходимо усилить контроль за соблюдением правил в классе</li>';
        recommendationsText += '<li>Провести работу по формированию уважительного отношения между учениками</li>';
        recommendationsText += '<li>Обратить внимание на случаи агрессивного поведения</li>';
        recommendationsText += '<li>Рассмотреть возможность привлечения школьного психолога</li>';
        recommendationsText += '</ul>';
    }
    
    if (results.wellbeingLevel === 'Низкий') {
        recommendationsText += '<p><strong>Низкий уровень благополучия:</strong></p>';
        recommendationsText += '<ul>';
        recommendationsText += '<li>Усилить работу по созданию позитивной атмосферы в классе</li>';
        recommendationsText += '<li>Провести мероприятия по сплочению коллектива</li>';
        recommendationsText += '<li>Обратить внимание на эмоциональное состояние учеников</li>';
        recommendationsText += '</ul>';
    }
    
    if (results.disunityLevel === 'Высокий' || results.disunityLevel === 'Очень высокий') {
        recommendationsText += '<p><strong>Высокий уровень разобщенности:</strong></p>';
        recommendationsText += '<ul>';
        recommendationsText += '<li>Провести работу по сплочению классного коллектива</li>';
        recommendationsText += '<li>Организовать совместные мероприятия и проекты</li>';
        recommendationsText += '<li>Улучшить взаимодействие между учениками и учителями</li>';
        recommendationsText += '</ul>';
    }
    
    if (results.equalityLevel === 'Низкий') {
        recommendationsText += '<p><strong>Низкий уровень равноправия:</strong></p>';
        recommendationsText += '<ul>';
        recommendationsText += '<li>Работать над принятием различий между учениками</li>';
        recommendationsText += '<li>Формировать толерантное отношение в классе</li>';
        recommendationsText += '<li>Развивать навыки конструктивного общения</li>';
        recommendationsText += '</ul>';
    }
    
    summary.innerHTML = summaryText;
    recommendations.innerHTML = recommendationsText;
}

function restartBullyingTest() {
    startBullyingTest();
}

// Обработка кнопки "Назад" в Telegram
tg.BackButton.onClick(() => {
    if (document.getElementById('san-test-screen').classList.contains('active')) {
        showScreen('user-info-screen');
        fillUserInfoForm();
    } else if (document.getElementById('psm25-test-screen').classList.contains('active')) {
        showScreen('user-info-screen');
        fillUserInfoForm();
    } else if (document.getElementById('burnout-test-screen').classList.contains('active')) {
        showScreen('user-info-screen');
        fillUserInfoForm();
    } else if (document.getElementById('anxiety-test-screen').classList.contains('active')) {
        showScreen('user-info-screen');
        fillUserInfoForm();
    } else if (document.getElementById('depression-test-screen').classList.contains('active')) {
        showScreen('user-info-screen');
        fillUserInfoForm();
    } else if (document.getElementById('bullying-test-screen').classList.contains('active')) {
        showScreen('user-info-screen');
        fillUserInfoForm();
    } else if (document.getElementById('aggression-test-screen').classList.contains('active')) {
        showScreen('user-info-screen');
        fillUserInfoForm();
    } else if (document.getElementById('results-screen').classList.contains('active')) {
        showScreen('burnout-stress-depression-screen');
    } else if (document.getElementById('psm25-results-screen').classList.contains('active')) {
        showScreen('burnout-stress-depression-screen');
    } else if (document.getElementById('burnout-results-screen').classList.contains('active')) {
        showScreen('burnout-stress-depression-screen');
    } else if (document.getElementById('anxiety-results-screen').classList.contains('active')) {
        showScreen('burnout-stress-depression-screen');
    } else if (document.getElementById('depression-results-screen').classList.contains('active')) {
        showScreen('burnout-stress-depression-screen');
    } else if (document.getElementById('bullying-results-screen').classList.contains('active')) {
        showScreen('bullying-screen');
    } else if (document.getElementById('aggression-results-screen').classList.contains('active')) {
        showScreen('bullying-screen');
    } else if (document.getElementById('user-info-screen').classList.contains('active')) {
        showScreen('burnout-stress-depression-screen');
    } else if (document.getElementById('burnout-stress-depression-screen').classList.contains('active')) {
        showMainScreen();
    } else if (document.getElementById('bullying-screen').classList.contains('active')) {
        showMainScreen();
    } else if (document.getElementById('consultation-screen').classList.contains('active')) {
        showMainScreen();
    }
});

// Показать кнопку "Назад" когда нужно
function updateBackButton() {
    const currentScreen = document.querySelector('.screen.active');
    if (currentScreen.id === 'main-screen') {
        // Скрыть кнопку "Назад" только на главном экране
        tg.BackButton.hide();
    } else {
        tg.BackButton.show();
    }
}

// Обновлять кнопку "Назад" при смене экранов
const originalShowScreen = showScreen;
showScreen = function(screenId) {
    originalShowScreen(screenId);
    updateBackButton();
};

// Дополнительные обработчики событий для тестов
document.addEventListener('DOMContentLoaded', function() {
    // Кнопки навигации теста выгорания
    document.getElementById('burnout-next-question').addEventListener('click', nextBurnoutQuestion);
    document.getElementById('burnout-prev-question').addEventListener('click', prevBurnoutQuestion);
    document.getElementById('burnout-finish-test').addEventListener('click', finishBurnoutTest);
    
    // Кнопки навигации теста тревоги
    document.getElementById('anxiety-next-question').addEventListener('click', nextAnxietyQuestion);
    document.getElementById('anxiety-prev-question').addEventListener('click', prevAnxietyQuestion);
    document.getElementById('anxiety-finish-test').addEventListener('click', finishAnxietyTest);
    
    // Кнопки навигации теста депрессии
    document.getElementById('depression-next-question').addEventListener('click', nextDepressionQuestion);
    document.getElementById('depression-prev-question').addEventListener('click', prevDepressionQuestion);
    document.getElementById('depression-finish-test').addEventListener('click', finishDepressionTest);
    
    // Кнопки навигации теста на буллинг
    document.getElementById('bullying-next-question').addEventListener('click', nextBullyingQuestion);
    document.getElementById('bullying-prev-question').addEventListener('click', prevBullyingQuestion);
    document.getElementById('bullying-finish-test').addEventListener('click', finishBullyingTest);
    
    // Кнопки результатов теста на буллинг
    document.getElementById('bullying-back-to-menu').addEventListener('click', showMainScreen);
    document.getElementById('bullying-restart-test').addEventListener('click', restartBullyingTest);
    
    // Кнопки навигации теста Д. Олвеуса
    document.getElementById('olweus-bullying-next-question').addEventListener('click', nextOlweusBullyingQuestion);
    document.getElementById('olweus-bullying-prev-question').addEventListener('click', prevOlweusBullyingQuestion);
    document.getElementById('olweus-bullying-finish-test').addEventListener('click', finishOlweusBullyingTest);
    
    // Кнопки результатов теста Д. Олвеуса
    document.getElementById('olweus-bullying-back-to-menu').addEventListener('click', showMainScreen);
    document.getElementById('olweus-bullying-restart-test').addEventListener('click', restartOlweusBullyingTest);
    
    // Кнопки навигации теста агрессивного поведения
    document.getElementById('aggression-next-question').addEventListener('click', nextAggressionQuestion);
    document.getElementById('aggression-prev-question').addEventListener('click', prevAggressionQuestion);
    document.getElementById('aggression-finish-test').addEventListener('click', finishAggressionTest);
    
    // Кнопки результатов теста агрессивного поведения
    document.getElementById('aggression-back-to-menu').addEventListener('click', showMainScreen);
    document.getElementById('aggression-restart-test').addEventListener('click', restartAggressionTest);
});

// Функции для теста Д. Олвеуса "Буллинг"
function startOlweusBullyingTest() {
    olweusBullyingCurrentQuestion = 0;
    olweusBullyingAnswers = {};
    showScreen('olweus-bullying-test-screen');
    displayOlweusBullyingQuestion();
    updateOlweusBullyingProgress();
    updateOlweusBullyingNavigationButtons();
}

function displayOlweusBullyingQuestion() {
    const question = olweusBullyingTestData.questions[olweusBullyingCurrentQuestion];
    const container = document.getElementById('olweus-bullying-question-container');
    
    container.innerHTML = `
        <div class="question">
            <div class="question-text">${question.text}</div>
            <div class="olweus-radio-scale">
                <label class="olweus-radio-option">
                    <input type="radio" name="olweus-bullying-answer" value="0" data-value="0">
                    <span class="olweus-radio-text">Никогда не было</span>
                </label>
                <label class="olweus-radio-option">
                    <input type="radio" name="olweus-bullying-answer" value="1" data-value="1">
                    <span class="olweus-radio-text">Было раз или два</span>
                </label>
                <label class="olweus-radio-option">
                    <input type="radio" name="olweus-bullying-answer" value="2" data-value="2">
                    <span class="olweus-radio-text">Бывает иногда</span>
                </label>
                <label class="olweus-radio-option">
                    <input type="radio" name="olweus-bullying-answer" value="3" data-value="3">
                    <span class="olweus-radio-text">Бывает раз в неделю</span>
                </label>
                <label class="olweus-radio-option">
                    <input type="radio" name="olweus-bullying-answer" value="4" data-value="4">
                    <span class="olweus-radio-text">Бывает несколько раз в неделю</span>
                </label>
            </div>
        </div>
    `;
    
    // Добавить обработчики событий для радиокнопок
    container.querySelectorAll('input[name="olweus-bullying-answer"]').forEach(radio => {
        radio.addEventListener('change', function() {
            if (this.checked) {
                // Сохранить ответ
                const value = parseInt(this.value);
                olweusBullyingAnswers[question.id] = value;
                
                // Обновить кнопки навигации
                updateOlweusBullyingNavigationButtons();
            }
        });
    });
    
    // Восстановить выбранный ответ, если он есть
    if (olweusBullyingAnswers[question.id] !== undefined) {
        const selectedRadio = container.querySelector(`input[value="${olweusBullyingAnswers[question.id]}"]`);
        if (selectedRadio) {
            selectedRadio.checked = true;
        }
    }
}

function updateOlweusBullyingProgress() {
    const totalQuestions = olweusBullyingTestData.questions.length;
    const currentQuestionNumber = olweusBullyingCurrentQuestion + 1;
    
    const progress = (currentQuestionNumber / totalQuestions) * 100;
    document.getElementById('olweus-bullying-progress').style.width = progress + '%';
    document.getElementById('olweus-bullying-current-question').textContent = currentQuestionNumber;
}

function updateOlweusBullyingNavigationButtons() {
    const prevBtn = document.getElementById('olweus-bullying-prev-question');
    const nextBtn = document.getElementById('olweus-bullying-next-question');
    const finishBtn = document.getElementById('olweus-bullying-finish-test');
    
    const isLastQuestion = olweusBullyingCurrentQuestion === olweusBullyingTestData.questions.length - 1;
    const currentQuestion = olweusBullyingTestData.questions[olweusBullyingCurrentQuestion];
    
    prevBtn.disabled = olweusBullyingCurrentQuestion === 0;
    
    if (isLastQuestion) {
        nextBtn.style.display = 'none';
        finishBtn.style.display = 'inline-block';
        finishBtn.disabled = olweusBullyingAnswers[currentQuestion.id] === undefined;
    } else {
        nextBtn.style.display = 'inline-block';
        finishBtn.style.display = 'none';
        nextBtn.disabled = olweusBullyingAnswers[currentQuestion.id] === undefined;
    }
}

function nextOlweusBullyingQuestion() {
    if (olweusBullyingCurrentQuestion < olweusBullyingTestData.questions.length - 1) {
        olweusBullyingCurrentQuestion++;
        displayOlweusBullyingQuestion();
        updateOlweusBullyingProgress();
        updateOlweusBullyingNavigationButtons();
    }
}

function prevOlweusBullyingQuestion() {
    if (olweusBullyingCurrentQuestion > 0) {
        olweusBullyingCurrentQuestion--;
        displayOlweusBullyingQuestion();
        updateOlweusBullyingProgress();
        updateOlweusBullyingNavigationButtons();
    }
}

function finishOlweusBullyingTest() {
    calculateOlweusBullyingResults();
    displayOlweusBullyingResults();
    
    // Сохранить статус прохождения теста
    const completedTests = JSON.parse(localStorage.getItem('completedTests') || '{}');
    completedTests['olweus-bullying'] = true;
    localStorage.setItem('completedTests', JSON.stringify(completedTests));
    
    // Отправляем результаты в Telegram
    const results = calculateOlweusBullyingResults();
    sendTestResultsToTelegram(results, userInfo, 'Опросник Д. Олвеуса «Буллинг»');
    
    showScreen('olweus-bullying-results-screen');
}

function calculateOlweusBullyingResults() {
    const results = {
        directActive: 0,
        indirectActive: 0,
        directVictim: 0,
        indirectVictim: 0
    };
    
    // Подсчет баллов по шкалам
    Object.keys(olweusBullyingTestData.scales).forEach(scaleKey => {
        const scale = olweusBullyingTestData.scales[scaleKey];
        let sum = 0;
        let count = 0;
        
        scale.questions.forEach(questionId => {
            const answer = olweusBullyingAnswers[questionId];
            if (answer !== undefined) {
                sum += answer;
                count++;
            }
        });
        
        const average = count > 0 ? sum / count : 0;
        
        switch(scaleKey) {
            case 'direct_active':
                results.directActive = average;
                break;
            case 'indirect_active':
                results.indirectActive = average;
                break;
            case 'direct_victim':
                results.directVictim = average;
                break;
            case 'indirect_victim':
                results.indirectVictim = average;
                break;
        }
    });
    
    // Общие шкалы
    results.activeBullying = (results.directActive + results.indirectActive) / 2;
    results.passiveBullying = (results.directVictim + results.indirectVictim) / 2;
    
    // Определить уровни выраженности
    results.activeBullyingLevel = getOlweusBullyingLevel(results.activeBullying);
    results.directActiveLevel = getOlweusBullyingLevel(results.directActive);
    results.indirectActiveLevel = getOlweusBullyingLevel(results.indirectActive);
    results.passiveBullyingLevel = getOlweusBullyingLevel(results.passiveBullying);
    results.directVictimLevel = getOlweusBullyingLevel(results.directVictim);
    results.indirectVictimLevel = getOlweusBullyingLevel(results.indirectVictim);
    
    return results;
}

function displayOlweusBullyingResults() {
    const results = calculateOlweusBullyingResults();
    
    // Отобразить баллы
    document.getElementById('olweus-active-bullying-score').textContent = results.activeBullying.toFixed(2);
    document.getElementById('olweus-direct-active-score').textContent = results.directActive.toFixed(2);
    document.getElementById('olweus-indirect-active-score').textContent = results.indirectActive.toFixed(2);
    document.getElementById('olweus-passive-bullying-score').textContent = results.passiveBullying.toFixed(2);
    document.getElementById('olweus-direct-victim-score').textContent = results.directVictim.toFixed(2);
    document.getElementById('olweus-indirect-victim-score').textContent = results.indirectVictim.toFixed(2);
    
    // Определить уровни выраженности
    const activeLevel = getOlweusBullyingLevel(results.activeBullying);
    const directActiveLevel = getOlweusBullyingLevel(results.directActive);
    const indirectActiveLevel = getOlweusBullyingLevel(results.indirectActive);
    const passiveLevel = getOlweusBullyingLevel(results.passiveBullying);
    const directVictimLevel = getOlweusBullyingLevel(results.directVictim);
    const indirectVictimLevel = getOlweusBullyingLevel(results.indirectVictim);
    
    // Отобразить уровни
    document.getElementById('olweus-active-bullying-level').textContent = activeLevel;
    document.getElementById('olweus-direct-active-level').textContent = directActiveLevel;
    document.getElementById('olweus-indirect-active-level').textContent = indirectActiveLevel;
    document.getElementById('olweus-passive-bullying-level').textContent = passiveLevel;
    document.getElementById('olweus-direct-victim-level').textContent = directVictimLevel;
    document.getElementById('olweus-indirect-victim-level').textContent = indirectVictimLevel;
    
    // Добавить CSS классы для уровней
    setOlweusBullyingLevelClass('olweus-active-bullying-level', activeLevel);
    setOlweusBullyingLevelClass('olweus-direct-active-level', directActiveLevel);
    setOlweusBullyingLevelClass('olweus-indirect-active-level', indirectActiveLevel);
    setOlweusBullyingLevelClass('olweus-passive-bullying-level', passiveLevel);
    setOlweusBullyingLevelClass('olweus-direct-victim-level', directVictimLevel);
    setOlweusBullyingLevelClass('olweus-indirect-victim-level', indirectVictimLevel);
    
    // Сводка результатов
    const summary = generateOlweusBullyingSummary(results);
    document.getElementById('olweus-bullying-summary').innerHTML = summary;
    
    // Рекомендации
    const recommendations = generateOlweusBullyingRecommendations(results);
    document.getElementById('olweus-bullying-recommendations').innerHTML = recommendations;
}

function getOlweusBullyingLevel(score) {
    if (score <= 1) return 'Слабо выражен';
    if (score < 3) return 'Умеренно выражен';
    return 'Ярко выражен';
}

function setOlweusBullyingLevelClass(elementId, level) {
    const element = document.getElementById(elementId);
    element.className = 'level-indicator';
    
    if (level === 'Слабо выражен') {
        element.classList.add('low');
    } else if (level === 'Умеренно выражен') {
        element.classList.add('medium');
    } else if (level === 'Ярко выражен') {
        element.classList.add('high');
    }
}

function generateOlweusBullyingSummary(results) {
    let summary = '<h3>Интерпретация результатов</h3>';
    
    summary += '<p><strong>Активный буллинг (проявление агрессии):</strong> ';
    if (results.activeBullying <= 1) {
        summary += 'Низкий уровень проявления агрессивного поведения.';
    } else if (results.activeBullying < 3) {
        summary += 'Умеренный уровень проявления агрессивного поведения.';
    } else {
        summary += 'Высокий уровень проявления агрессивного поведения.';
    }
    summary += '</p>';
    
    summary += '<p><strong>Пассивный буллинг (виктимизация):</strong> ';
    if (results.passiveBullying <= 1) {
        summary += 'Низкий уровень подверженности буллингу.';
    } else if (results.passiveBullying < 3) {
        summary += 'Умеренный уровень подверженности буллингу.';
    } else {
        summary += 'Высокий уровень подверженности буллингу.';
    }
    summary += '</p>';
    
    return summary;
}

function generateOlweusBullyingRecommendations(results) {
    let recommendations = '<h3>Рекомендации</h3><ul>';
    
    if (results.activeBullying > 1) {
        recommendations += '<li>Обратитесь к психологу для работы с агрессивным поведением</li>';
        recommendations += '<li>Изучите техники управления гневом и конфликтами</li>';
        recommendations += '<li>Развивайте эмпатию и навыки общения</li>';
    }
    
    if (results.passiveBullying > 1) {
        recommendations += '<li>Обратитесь за поддержкой к взрослым (родителям, учителям, психологу)</li>';
        recommendations += '<li>Развивайте уверенность в себе и навыки самозащиты</li>';
        recommendations += '<li>Ищите поддержку у друзей и близких</li>';
    }
    
    if (results.activeBullying <= 1 && results.passiveBullying <= 1) {
        recommendations += '<li>Продолжайте поддерживать здоровые отношения с окружающими</li>';
        recommendations += '<li>Будьте внимательны к другим и готовы помочь тем, кто может нуждаться в поддержке</li>';
    }
    
    recommendations += '</ul>';
    return recommendations;
}

function restartOlweusBullyingTest() {
    startOlweusBullyingTest();
}

// Функции для теста агрессивного поведения
function startAggressionTest() {
    aggressionCurrentQuestion = 0;
    aggressionAnswers = {};
    showScreen('aggression-test-screen');
    displayAggressionQuestion();
    updateAggressionProgress();
    updateAggressionNavigationButtons();
}

function displayAggressionQuestion() {
    const question = aggressionTestData.questions[aggressionCurrentQuestion];
    const container = document.getElementById('aggression-question-container');
    
    container.innerHTML = `
        <div class="question">
            <div class="question-text">${question.text}</div>
            <div class="aggression-radio-scale">
                <label class="aggression-radio-option">
                    <input type="radio" name="aggression-answer" value="1">
                    <span class="aggression-radio-text">Да</span>
                </label>
                <label class="aggression-radio-option">
                    <input type="radio" name="aggression-answer" value="0">
                    <span class="aggression-radio-text">Нет</span>
                </label>
            </div>
        </div>
    `;
    
    // Добавляем обработчики событий
    container.querySelectorAll('input[name="aggression-answer"]').forEach(radio => {
        radio.addEventListener('change', function() {
            if (this.checked) {
                aggressionAnswers[question.id] = parseInt(this.value);
                updateAggressionNavigationButtons();
            }
        });
    });
    
    // Восстанавливаем предыдущий ответ, если есть
    if (aggressionAnswers[question.id] !== undefined) {
        const selectedRadio = container.querySelector(`input[value="${aggressionAnswers[question.id]}"]`);
        if (selectedRadio) {
            selectedRadio.checked = true;
        }
    }
}

function updateAggressionProgress() {
    const totalQuestions = aggressionTestData.questions.length;
    const currentQuestionNumber = aggressionCurrentQuestion + 1;
    
    const progress = (currentQuestionNumber / totalQuestions) * 100;
    document.getElementById('aggression-progress').style.width = progress + '%';
    document.getElementById('aggression-current-question').textContent = currentQuestionNumber;
}

function updateAggressionNavigationButtons() {
    const prevBtn = document.getElementById('aggression-prev-question');
    const nextBtn = document.getElementById('aggression-next-question');
    const finishBtn = document.getElementById('aggression-finish-test');
    
    const isLastQuestion = aggressionCurrentQuestion === aggressionTestData.questions.length - 1;
    const currentQuestion = aggressionTestData.questions[aggressionCurrentQuestion];
    
    prevBtn.disabled = aggressionCurrentQuestion === 0;
    
    if (isLastQuestion) {
        nextBtn.style.display = 'none';
        finishBtn.style.display = 'inline-block';
        finishBtn.disabled = aggressionAnswers[currentQuestion.id] === undefined;
    } else {
        nextBtn.style.display = 'inline-block';
        finishBtn.style.display = 'none';
        nextBtn.disabled = aggressionAnswers[currentQuestion.id] === undefined;
    }
}

function nextAggressionQuestion() {
    if (aggressionCurrentQuestion < aggressionTestData.questions.length - 1) {
        aggressionCurrentQuestion++;
        displayAggressionQuestion();
        updateAggressionProgress();
        updateAggressionNavigationButtons();
    }
}

function prevAggressionQuestion() {
    if (aggressionCurrentQuestion > 0) {
        aggressionCurrentQuestion--;
        displayAggressionQuestion();
        updateAggressionProgress();
        updateAggressionNavigationButtons();
    }
}

function finishAggressionTest() {
    calculateAggressionResults();
    displayAggressionResults();
    
    // Сохраняем результаты
    const results = calculateAggressionResults();
    
    // Сохранить статус прохождения теста
    const completedTests = JSON.parse(localStorage.getItem('completedTests') || '{}');
    completedTests.aggression = true;
    localStorage.setItem('completedTests', JSON.stringify(completedTests));
    
    // Отправляем результаты в Telegram
    sendTestResultsToTelegram(results, userInfo, 'Диагностика агрессивного поведения');
    
    showScreen('aggression-results-screen');
}

function calculateAggressionResults() {
    const results = {
        verbal: 0,
        physical: 0,
        object: 0,
        emotional: 0,
        self: 0,
        total: 0
    };
    
    // Подсчет баллов по каждой шкале
    Object.keys(aggressionTestData.scoringKey).forEach(scaleKey => {
        const scale = aggressionTestData.scoringKey[scaleKey];
        let score = 0;
        
        // Подсчет баллов за ответы "Да"
        scale.yes.forEach(questionId => {
            if (aggressionAnswers[questionId] === 1) {
                score++;
            }
        });
        
        // Подсчет баллов за ответы "Нет"
        scale.no.forEach(questionId => {
            if (aggressionAnswers[questionId] === 0) {
                score++;
            }
        });
        
        results[scaleKey] = score;
    });
    
    // Общий балл
    results.total = results.verbal + results.physical + results.object + results.emotional + results.self;
    
    return results;
}

function displayAggressionResults() {
    const results = calculateAggressionResults();
    
    // Отображаем баллы
    document.getElementById('aggression-verbal-score').textContent = results.verbal;
    document.getElementById('aggression-physical-score').textContent = results.physical;
    document.getElementById('aggression-object-score').textContent = results.object;
    document.getElementById('aggression-emotional-score').textContent = results.emotional;
    document.getElementById('aggression-self-score').textContent = results.self;
    document.getElementById('aggression-total-score').textContent = results.total;
    
    // Определяем уровни
    const verbalLevel = getAggressionLevel('verbal', results.verbal);
    const physicalLevel = getAggressionLevel('physical', results.physical);
    const objectLevel = getAggressionLevel('object', results.object);
    const emotionalLevel = getAggressionLevel('emotional', results.emotional);
    const selfLevel = getAggressionLevel('self', results.self);
    const totalLevel = getAggressionTotalLevel(results.total);
    
    // Обновляем индикаторы уровней
    document.getElementById('aggression-verbal-level').textContent = verbalLevel.text;
    document.getElementById('aggression-verbal-level').className = `level-indicator ${verbalLevel.class}`;
    
    document.getElementById('aggression-physical-level').textContent = physicalLevel.text;
    document.getElementById('aggression-physical-level').className = `level-indicator ${physicalLevel.class}`;
    
    document.getElementById('aggression-object-level').textContent = objectLevel.text;
    document.getElementById('aggression-object-level').className = `level-indicator ${objectLevel.class}`;
    
    document.getElementById('aggression-emotional-level').textContent = emotionalLevel.text;
    document.getElementById('aggression-emotional-level').className = `level-indicator ${emotionalLevel.class}`;
    
    document.getElementById('aggression-self-level').textContent = selfLevel.text;
    document.getElementById('aggression-self-level').className = `level-indicator ${selfLevel.class}`;
    
    document.getElementById('aggression-total-level').textContent = totalLevel.text;
    document.getElementById('aggression-total-level').className = `level-indicator ${totalLevel.class}`;
    
    // Генерируем сводку и рекомендации
    generateAggressionSummary(results);
}

function getAggressionLevel(scale, score) {
    if (score > 5) {
        return { text: 'Высокая степень агрессивности', class: 'high' };
    } else if (score >= 3) {
        return { text: 'Средняя степень агрессивности', class: 'medium' };
    } else {
        return { text: 'Низкая степень агрессивности', class: 'low' };
    }
}

function getAggressionTotalLevel(totalScore) {
    if (totalScore > 25) {
        return { text: 'Высокая степень агрессивности', class: 'high' };
    } else if (totalScore >= 11) {
        return { text: 'Средний уровень агрессивности', class: 'medium' };
    } else {
        return { text: 'Низкая степень агрессивности', class: 'low' };
    }
}

function generateAggressionSummary(results) {
    const summary = document.getElementById('aggression-summary');
    const recommendations = document.getElementById('aggression-recommendations');
    
    // Скрываем блок интерпретации, так как результаты уже отображаются в красивых блоках
    summary.style.display = 'none';
    
    // Рекомендации
    let recommendationsText = '<h3>Рекомендации:</h3><ul>';
    
    if (results.total > 25) {
        recommendationsText += '<li>Рекомендуется консультация с психологом для работы с агрессивным поведением</li>';
        recommendationsText += '<li>Изучение техник управления гневом и стрессом</li>';
        recommendationsText += '<li>Развитие навыков конструктивного общения</li>';
    } else if (results.total >= 11) {
        recommendationsText += '<li>Обратите внимание на развитие навыков самоконтроля</li>';
        recommendationsText += '<li>Изучение техник релаксации и медитации</li>';
        recommendationsText += '<li>Практика конструктивного решения конфликтов</li>';
    } else {
        recommendationsText += '<li>Продолжайте развивать навыки адаптивного поведения</li>';
        recommendationsText += '<li>Поддерживайте здоровые способы выражения эмоций</li>';
    }
    
    recommendationsText += '</ul>';
    recommendations.innerHTML = recommendationsText;
}

function restartAggressionTest() {
    startAggressionTest();
}

// Функции для формы консультации
function initConsultationForm() {
    const form = document.getElementById('consultation-form');
    const problemTextarea = document.getElementById('consultation-problem');
    const charCount = document.getElementById('problem-char-count');
    const submitBtn = document.getElementById('consultation-submit-btn');
    
    // Обработчик изменения текста в textarea
    problemTextarea.addEventListener('input', function() {
        const currentLength = this.value.length;
        charCount.textContent = currentLength;
        
        // Изменяем цвет счетчика в зависимости от количества символов
        charCount.className = 'char-counter';
        if (currentLength > 800) {
            charCount.classList.add('error');
        } else if (currentLength > 600) {
            charCount.classList.add('warning');
        }
    });
    
    // Обработчик отправки формы
    form.addEventListener('submit', async function(e) {
        e.preventDefault();
        
        // Получаем данные формы
        const formData = new FormData(form);
        const consultationData = {
            name: formData.get('name').trim(),
            phone: formData.get('phone').trim(),
            problem: formData.get('problem').trim()
        };
        
        // Валидация
        if (!consultationData.name || !consultationData.phone || !consultationData.problem) {
            alert('Пожалуйста, заполните все обязательные поля');
            return;
        }
        
        // Валидация телефона (простая проверка)
        const phoneRegex = /^[\+]?[0-9\s\-\(\)]{10,}$/;
        if (!phoneRegex.test(consultationData.phone)) {
            alert('Пожалуйста, введите корректный номер телефона');
            return;
        }
        
        // Блокируем кнопку отправки
        submitBtn.disabled = true;
        submitBtn.textContent = 'Отправка...';
        
        try {
            // Отправляем данные в Telegram
            await sendConsultationToTelegram(consultationData);
            
            // Показываем сообщение об успехе
            showConsultationSuccess();
            
        } catch (error) {
            console.error('Ошибка при отправке заявки:', error);
            alert('Произошла ошибка при отправке заявки. Попробуйте еще раз.');
            
            // Разблокируем кнопку
            submitBtn.disabled = false;
            submitBtn.textContent = 'Записаться на консультацию';
        }
    });
}

// Функция отправки данных консультации в Telegram
async function sendConsultationToTelegram(consultationData) {
    try {
        // Получаем информацию о пользователе из Telegram
        const telegramUser = tg.initDataUnsafe?.user;
        const userName = telegramUser ? `${telegramUser.first_name} ${telegramUser.last_name || ''}`.trim() : 'Неизвестный пользователь';
        const userId = telegramUser?.id || 'unknown';
        const username = telegramUser?.username ? `@${telegramUser.username}` : 'Нет username';
        
        const message = `
📋 <b>Новая заявка на консультацию</b>

👤 <b>Данные пользователя:</b>
• Имя: ${userName}
• Username: ${username}
• Telegram ID: ${userId}

📝 <b>Данные заявки:</b>
• Имя: ${consultationData.name}
• Телефон: ${consultationData.phone}
• Проблема: ${consultationData.problem}

⏰ Время подачи заявки: ${new Date().toLocaleString('ru-RU')}
🔗 Источник: Telegram WebApp`;

        // Отправляем сообщение всем админам
        const sendPromises = CONFIG.ADMIN_CHAT_IDS.map(async (adminId) => {
            const data = {
                chat_id: adminId,
                text: message,
                parse_mode: 'HTML'
            };
            return await sendToTelegram(data);
        });

        try {
            const responses = await Promise.all(sendPromises);
            const successCount = responses.filter(response => response !== null).length;
            console.log(`Заявка на консультацию успешно отправлена ${successCount} из ${CONFIG.ADMIN_CHAT_IDS.length} админов в Telegram`);
        } catch (error) {
            console.error('Ошибка при отправке заявки админам:', error);
        }
    } catch (error) {
        console.error('Ошибка при отправке заявки на консультацию:', error);
        throw error;
    }
}

// Функция показа сообщения об успешной отправке
function showConsultationSuccess() {
    const form = document.getElementById('consultation-form');
    const successMessage = document.getElementById('consultation-success');
    
    form.style.display = 'none';
    successMessage.style.display = 'block';
    
    // Анимация появления
    successMessage.style.opacity = '0';
    successMessage.style.transform = 'translateY(20px)';
    
    setTimeout(() => {
        successMessage.style.transition = 'all 0.5s ease';
        successMessage.style.opacity = '1';
        successMessage.style.transform = 'translateY(0)';
    }, 100);
}

// Функция сброса формы консультации
function resetConsultationForm() {
    const form = document.getElementById('consultation-form');
    const successMessage = document.getElementById('consultation-success');
    const submitBtn = document.getElementById('consultation-submit-btn');
    const charCount = document.getElementById('problem-char-count');
    
    form.reset();
    form.style.display = 'block';
    successMessage.style.display = 'none';
    
    submitBtn.disabled = false;
    submitBtn.textContent = 'Записаться на консультацию';
    charCount.textContent = '0';
    charCount.className = 'char-counter';
}

